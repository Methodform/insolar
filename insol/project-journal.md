# Инсоляр — полный журнал проекта (источник правды)

> Единый документ по всей работе над сервисом: что построено, какие решения приняты,
> архитектура, стратегия, деплой, открытые задачи. Обновляется по мере работы.
>
> **Последнее обновление:** 2026-07-23 13:23 (+04)
> **Режим обновления:** ежечасно до 2026-07-22 23:00 (см. раздел «Регламент обновления»).

---

## 1. Что за продукт

**Инсоляр** — веб-сервис моделирования положения и движения солнца, теней и инсоляции
земельного участка (ИЖС/дача) в реальном времени. Пользователь задаёт участок,
расставляет постройки и озеленение, видит тени и инсоляцию, получает документ.

**Ядро ценности:** не «моделирование солнца», а снятие риска дорогой необратимой ошибки
(снос, спор с соседом, сад в тени) и получение уверенного, задокументированного решения
до начала стройки. Отличие от конкурентов — **живой интерактив** (двигаешь дом → тени
пересчитываются), а не статичный отчёт.

Живёт по адресу: https://methodform.github.io/insolar/ (ванильная версия — корень,
React-версия — `/app/`).

---

## 2. Архитектура и стек

Две параллельные версии в одном репозитории (Methodform/insolar):

- **Ванильная** (`index.html`, ~2600 строк) — single-file HTML/CSS/JS + Three.js r128 (CDN).
  Работает на GitHub Pages без сборки. Полный референс всех фич.
- **React + Radix** (`react-app/`) — Vite + React 18 + @radix-ui/themes + @radix-ui/react-icons
  + three@0.160 + maplibre-gl. Собирается в CI, деплоится в `/app/`. Идёт портирование к паритету.

Маркетинг — статические HTML: `landing.html` (лендинг, JTBD, калькулятор экономии,
переключатель лето/зима с фото), `blog.html` (блог), `article-insolyaciya.html` (первая статья).

### Структура React-приложения
- `src/App.jsx` — оболочка Radix, всё состояние и панели.
- `src/engine/astronomy.js` — чистый JS-движок (астрономия + геометрия + инсоляция), без Three.
- `src/three/Viewport.jsx` — 3D-сцена (Three.js): солнце, тени, здания, гизмо, аналитика, карта-подложка, маркеры.
- `src/three/PlanEditor.jsx` — редактор плана (вид сверху): прямоугольник/контур/дорожка, подложка-чертёж.
- `src/three/MapView.jsx` — участок на реальной карте (MapLibre + OpenFreeMap).
- `src/three/SunPath.jsx` — диаграмма пути солнца (небесный купол: сезонные дуги, стороны света, форма участка в центре, траектория текущего дня под точкой солнца).
- `src/three/HeatMap.jsx` — годовая тепловая карта (canvas). *(в UI сейчас не подключена)*
- `src/three/ZoneMap.jsx` — схема рекомендаций зонирования.
- `cadastre-proxy/worker.js` — Cloudflare Worker: участок по кадастровому номеру (НСПД).

---

## 3. Движок расчётов (`astronomy.js`)

- Позиция солнца — алгоритм Meeus/SunCalc (в UTC, ввод/вывод в местном поясе).
- Инсоляция — метод теневого полигона (без raycast): выпуклая оболочка footprint ∪ проекции тени.
- Нормы СанПиН 1.2.3685-21: непрерывная инсоляция ≥ 2.5/2.0/1.5 ч по климатическим зонам
  (север ≥58°, центр 48–58°, юг <48°).
- Отступы СП 53.13330 / СП 42.13330: дом 3 м, прочее 1 м; противопожарные СП 4.13130.
- Геометрия: площадь, offsetInward (отступ внутрь), plotBasis (базис участка по длинной стороне),
  rectFromDrag (прямоугольник вдоль участка), clampToPoly.
- Окна: facadeWindows + windowInsolation — только для дома/бани и нарисованных зданий с крышей.
- Контрольные точки (reportData): сетка 5×5 (N=4) в базисе участка, внутри контура.

---

## 4. Полный список функций

**Базовые (Free):** участок по координатам точек или по кадастровому номеру; путь солнца;
3D-тени в реальном времени; забор (светло-серый, без толщины); здания/постройки/озеленение;
инсоляция в точке и по контрольным точкам участка (зелёные/красные маркеры); длина тени;
компас С/Ю/В/З в сцене; тема светлая/тёмная/системная; участок на реальной карте (схема).

**Модели (процедурные, «+ Типовое»):** дом, баня, беседка, навес, шатёр, дерево, куст.
Быстрая посадка деревьев/кустов кликом по участку. Дорожки — в режиме «Рисовать».

**Редактор плана («Рисовать»):** прямоугольник (параллельно сторонам участка), контур с
углами 90°, дорожки (ширина), перетаскивание/поворот(R)/удаление(Del), линии отступов 1/3 м,
сетка, подложка-чертёж (загрузка картинки).

**3D-взаимодействие:** гизмо перемещения (оси вдоль сторон участка), поворот, масштаб;
перетаскивание с ограничением по нормативным отступам (объект держится в участке).

**Pro:** 3D-аналитика поверхностей («тепловизор», плавный градиент по земле/фасадам/крышам/забору,
выбор месяцев, учёт рассеянного света, легенда); нормативный отчёт по СанПиН (PDF, в футере правой
панели, «Скачать PDF отчёт»); сохранение/загрузка проекта (JSON).

**Всегда доступно в шапке:** памятка нормативных отступов (модалка), рекомендации по зонированию.

**Карта:** окно «Показать на карте» — OpenFreeMap (схема, бесплатно, без ключа); подложка-схема
в 3D — растровые тайлы MapTiler по ключу (встроенному в сборку или введённому вручную).

---

## 5. Карты и внешние данные

- **Схема улиц:** OpenFreeMap (векторные тайлы OSM) — бесплатно, без ключа, коммерция ок. Рендер MapLibre.
- **Схема-подложка в 3D:** растровые тайлы MapTiler streets (бесплатный тариф, ключ). Static Maps API
  НЕ используем — он платный (был баг «Invalid key» из-за него).
- **Спутник:** удалён из UI по решению (оставили только схему).
- **Кадастр:** Cloudflare Worker → НСПД `api/geoportal/v2/search/geoportal`, геометрия EPSG:3857 → WGS84.
  Риск анти-бота НСПД; ручной ввод точек — всегда рабочий fallback.
- **Ключ MapTiler:** встраивается в сборку через `VITE_MAPTILER_KEY` (GitHub secret `MAPTILER_KEY`),
  защищается ограничением по домену. Поле ручного ввода — fallback, когда встроенного ключа нет.
  Полностью скрыть клиентский ключ нельзя; при росте — кэш тайлов, платный тариф или self-host.

---

## 6. Деплой

GitHub Pages + GitHub Actions (`.github/workflows/deploy.yml`):
сборка `react-app` (`npm install && npm run build` с `VITE_MAPTILER_KEY`), сборка `_site`
(ванильные `*.html`, `robots.txt`, `sitemap.xml`, `img/` в корень; `react-app/dist` → `/app/`),
деплой. Репозиторий публичный, источник Pages = GitHub Actions.

**Пуш через токен (пользователь делает сам, своим PAT):**
`git push "https://Methodform:ТОКЕН@github.com/Methodform/insolar.git" main:main`
Токен: classic PAT со scope `repo` + `workflow`; в чат не вставлять.

---

## 7. Стратегия (JTBD / GTM)

Подробно — в `jtbd-insolation-service.md` и `gtm-roadmap.md`.

- **Сегменты:** частник ИЖС (эмоц. джоба «где ставить, не ошибиться»), проектировщик
  (нормативный отчёт), риелтор (презентация участка).
- **Beachhead — частник ИЖС/дача:** продукт под него готов, массовый рынок, виральность, SEO.
- **Монетизация:** щедрый Free → платные подписки. Единый модал тарифов по клику на любую платную
  функцию. Линейка: **Месяц / Сезон (6 мес, рекомендуемый) / Год** плюс разовый «паспорт участка» по
  ссылке. Акцент на **Год как центр** — «живой планировщик круглый год» (посадил дом сейчас, вернулся
  весной пересадить сад). Под подпиской: объекты/постройки, выбор даты, 3D-аналитика, PDF-отчёт,
  сохранение, кадастр по номеру.
- **Позиционирование:** «не отчёт, купленный один раз, а живой участок, который можно переиграть —
  и в любой момент выгрузить паспорт или документ по СанПиН».
- **Ключевой козырь vs конкуренты (smotrisunduk и др.):** интерактив + шаринг живой ссылкой +
  нормативный СанПиН, чего у статичных «паспортов» нет.
- **Нейминг:** обсуждались варианты под .solar и standalone (Ярило, Погожий, moy.solar, sad.solar).
  Финал не зафиксирован; текущее рабочее — «Инсоляр».
- **Конкуренты:** СИТИС Солярис (нормативка, тяжёлый десктоп), Shadowmap/SunTrace3D (3D-тени, без
  СанПиН/кадастра), smotrisunduk (потребительский PDF-паспорт с розой ветров).

## 8. SEO
Сделана техническая база: title/description/OG/Twitter-карточки, JSON-LD (SoftwareApplication/Blog/
BlogPosting), `robots.txt`, `sitemap.xml` + копирование в сборку. Осталось (пользователю):
Яндекс.Вебмастер + Google Search Console + Яндекс.Метрика; контент-план 10–15 статей под кластеры
(«куда поставить дом», «отступы от забора», «тень от дома»); при своём домене — заменить canonical/og:url.

---

## 9. Хроника ключевых правок и решений

1. Базовый прототип: солнце/тени, участок по 4 координатам, забор 1.8/2.2 м.
2. Рисование зданий по точкам (углы 90°), дефолтные точки (Самара), пресеты построек, повороты, drag.
3. Красная линия отступов 1/3 м; длина тени от забора/зданий.
4. Тёмная/светлая тема; design-guideline по photoncycle.
5. Pro-функции: годовая тепловая карта, сохранение, инсоляция по окнам; нормативный отчёт PDF/DOCX.
6. 3D-аналитика поверхностей («тепловизор»), сглаженный градиент, распространение на забор.
7. Юр. аспект: отчёт — модельный, не заменяет аккредитованную экспертизу.
8. Мобильная упаковка обсуждалась; ограничение отступов при перетаскивании; памятка отступов.
9. Плавающие панели поверх вьюпорта, скругл控1, скрытый скроллбар, псевдо-облака (позже удалены).
10. Деплой на GitHub (Pages + Actions), решены проблемы с PAT/scope/Keychain/public repo.
11. Полный переезд на React + Radix; поэтапный порт: астрономия, здания, гизмо, инсоляция, путь солнца,
    Pro, тепловая карта, отчёт, сохранение, **ручное рисование**, окна, памятка, облака+компас,
    зонирование, ограничение отступов, иконки Radix, 3D-аналитика.
12. UI-итерации: убраны вкладки; непрозрачная белая шапка/панели (panelBackground solid);
    тема свет/тьма/система; убраны облака; тонкий серый забор; фикс теней (near/far, normalBias);
    PDF в футер правой панели; маркеры окон и участка; гизмо вдоль участка; убраны лишние рамки/линии.
13. Кадастр по номеру (Worker НСПД). Реальная карта (MapLibre+OpenFreeMap) + подложка в 3D
    (сначала Static Maps — платно/баг, затем сшивка растровых тайлов). Спутник убран, оставлена схема.
14. Стратегия: JTBD-разбор, GTM-роадмап (beachhead частник), нейминг, выбор бесплатных карт, SEO-база.
15. React: гейтинг Pro-функций (объекты/постройки, выбор даты, 3D-аналитика, PDF-отчёт, сохранение —
    под Pro); забор с выбором «Нет / 1.8 м / свой»; удаление объекта клавишей DEL в 3D; убраны
    «Реальное время» и быстрые посадки деревьев/кустов кликом.
16. React, тарифы и UI-чистка: единый модал тарифов по клику на платную функцию; итерации линейки —
    Free/месяц/год → Месяц/Сезон(6 мес, рекомендуемый)/Год + разовый паспорт ссылкой → «Год как центр»
    (живой планировщик круглый год); кадастр переведён под подписку; убраны тексты про Pro, замочки на
    кнопках, кнопки «Показать на карте» и «Сбросить», разделители панелей.
17. React: мобильная адаптация — шторки (bottom sheets) панелей, нижние вкладки навигации, тач/пинч-жесты
    в 3D-вьюпорте, компактная шапка. Уточнения: полноэкранные плоские шторки без скруглений; таб-бар из 4
    вкладок (Участок/Солнце/Советы/Профиль) с центральной кнопкой 3D; тема переключается кнопкой-иконкой;
    кнопка «Построить/Build» закрывает шторку.
18. Маркетинг-ресёрч: добавлен `suntrace3d-vs-shadowmap-analysis.md` — сравнение конкурентов
    Shadowmap (зрелый лидер, ~190K визитов/мес) и SunTrace3D (инди, угол «окупаемость солнечных панелей»);
    обновляется ежечасно.
19. React, чистка UI: убрана рамка псевдоэлемента `::after` у панельных карточек (устранена лишняя
    линия в 3D-аналитике и на мобильной вёрстке).
20. React, диаграмма пути солнца и полировка тарифов/навигации: `SunPath.jsx` переработан в «небесный
    купол» — сезонные дуги, стороны света, форма участка в центре, а траектория текущего дня рисуется
    прямо под точкой солнца. Тарифы: цена в одну строку с подстрочником второй строкой, убраны галочки,
    на мобиле карточки тарифов идут вертикально. Таб-бар на мобиле — без подложек (активная вкладка
    зелёная, неактивные тёмно-серые).

---

## 10. Известные баги и как решены
- `</script>` в шаблон-строке ломает печать → экранирование `<scr${''}ipt>`.
- Разрыв тени от забора/объектов → узкий near/far теневой камеры + меньше normalBias.
- Краш при добавлении дерева → `mesh.position` только для чтения; заменено на `.position.set()`.
- «Invalid key» на 3D-подложке → MapTiler Static Maps платный; перешли на растровые тайлы.
- «Тонкая серая линия» сквозь панель → panelBackground="solid".
- Тепловая карта «полосой» → отсечение солнца ниже 5° (длинные тени у горизонта).
- Локальная сборка React невозможна (нет node_modules) → проверка `node --check` + баланс скобок, сборка в CI.
- `clampToPoly` не был импортирован в `Viewport.jsx` (используется в `plant()`) → латентный ReferenceError при посадке дерева/куста кликом. Не проявлялся, т.к. `plantMode` всегда `null` (быстрые посадки убраны). Исправлено добавлением импорта из `astronomy.js`.

## 11. Открытые задачи / роадмап
- Порт-паритет React: спутниковая подложка (осознанно убрана), 3D-«тепловизор» — сделан.
- «Паспорт участка» (потребительский PDF): план с размерами сторон/периметром/масштабом,
  сезонная инсоляция (зима/равноденствие/лето), рекомендации в PDF, обложка, лист «для заметок».
- Шаринг живой ссылкой (участок+объекты в URL) — главный интерактивный рычаг роста.
- Сохранённые сценарии / сравнение вариантов A/B.
- Онбординг к aha-моменту; мобильная адаптация.
- Кэширование тайлов карты; при росте — платный ключ/self-host.
- Монетизация: биллинг РФ, риск-пейволл, кейсы.
- SEO-контент; подключение Вебмастера/Метрики; свой домен.

---

## Регламент обновления
Документ обновляется ежечасно до **2026-07-22 23:00 (+04)** запланированной задачей:
при каждом запуске в него вносятся новые изменения проекта и переписки; после дедлайна
задача самоудаляется. Файл: `insol/project-journal.md` (проект переехал в подпапку `insol/`, см. запись 19:02).

### Журнал автообновлений
- 2026-07-21 11:02 — без изменений с предыдущего обновления (последний коммит `0c5599e` — SEO; рабочих правок нет).
- 2026-07-21 12:02 — новый коммит `3f1e923`: в React добавлен гейтинг Pro (объекты, дата, аналитика, PDF, сохранение), забор Нет/1.8/свой, удаление DEL в 3D, убраны «Реальное время» и быстрые посадки. Внесено в разделы «Хроника» (п.15) и учтено в описании функций/Pro.
- 2026-07-21 14:11 — серия новых коммитов после `3f1e923` (`a1d2ea8`, `a059c36`, `6876654`, `0ec2461`, `ea1480b`, `f8628d3`): переработка тарифов (единый модал → линейка Месяц/Сезон/Год + разовый паспорт → «Год как центр»), кадастр под подписку, чистка UI (убраны замочки, тексты про Pro, кнопки «Показать на карте»/«Сбросить», разделители панелей) и мобильная адаптация (шторки, нижние вкладки, тач/пинч). Добавлен файл `suntrace3d-vs-shadowmap-analysis.md`. Обновлены «Монетизация» (§7) и «Хроника» (п.16–18).
- 2026-07-21 15:02 — два новых коммита по мобильной адаптации React: `9eb7722` (полноэкранные шторки, таб-бар из 4 вкладок Участок/Солнце/Советы/Профиль, тема кнопкой-иконкой) и `1ae3b81` (кнопка Build закрывает шторку, плоские шторки без скруглений, центральная кнопка 3D). Оба — правки `react-app/src/App.jsx`. Уточнён п.17 «Хроника».
- 2026-07-21 16:02 — новый коммит `984244c`: убрана рамка псевдоэлемента `::after` у панельных карточек (лишняя линия в 3D-аналитике и на мобиле). Добавлен п.19 в «Хронику».
- 2026-07-21 17:51 — **обновление GTM-стратегии** по двум каналам Михаила Табунова: [@your_pet_project](https://t.me/your_pet_project) и [@bossofyourboss](https://t.me/bossofyourboss). Переписан `gtm-roadmap.md`: добавлен раздел 0 «Методология запуска» (10 принципов — DoD-позиционирование, правило 180 секунд, трафик-первый/поиск, отсекать зевак, fail-fast «отрубай», разовая vs повторяющаяся боль, бенчмарки ретеншена, экономика CAC/ROAS, доводка вайб-кода руками + буфер 80/25, пруфы). Ключевой сдвиг монетизации: у частника боль **разовая** → основной доход = **разовый платный «отчёт-паспорт» (490–990 ₽)**, подписка вторична и живёт на профи/риелторах (повторяющаяся боль). Переработаны разделы позиционирования (DoD-заголовок), каналов (сначала поиск), метрик (+ критерии «отрубить»), рисков и «5 шагов недели» (под первые продажи). Продукт признан по сути готовым — центр тяжести смещён на трафик и первые 180 секунд.
- 2026-07-21 17:41 — проверка работоспособности React-версии (по запросу, без сравнения с ванильной). Установлены зависимости, `vite build` проходит (290 модулей). Полный обзор App.jsx, Viewport/PlanEditor/SunPath/MapView/ZoneMap, движка. Найден и исправлен один реальный баг: `clampToPoly` не импортировался в `Viewport.jsx` (латентный ReferenceError в `plant()`; сейчас не стреляет — `plantMode` всегда null). Остальные компоненты и движок — без ошибок. Прим.: удаление `dist/` на смонтированном диске даёт EPERM — особенность окружения, не кода; в CI сборка с нуля. Замечен мусорный `react-app/src/.fuse_hidden0000000400000001` (хвост редактора, ничем не импортируется) — стоит удалить.
- 2026-07-21 18:02 — доучтены пять коммитов 16:33–16:52, ранее не попавшие в «Хронику» (записи 17:41/17:51 были про здоровье React и GTM): `e1e0f3e` (диаграмма пути солнца → небесный купол: сезонные дуги, стороны света, форма участка), `1d3730a` (моб. таб-бар без подложек — активная зелёная, неактивные тёмно-серые), `9672f02` (цена тарифа в одну строку + подстрочник), `435062e` (тарифы без галочек, на мобиле карточки вертикально), `e4532ee` (траектория текущего дня под точкой солнца, участок по центру). Обновлены §2 (описание `SunPath.jsx`) и «Хроника» (п.20).
- 2026-07-21 19:02 — новых коммитов с 18:02 нет (HEAD = `e4532ee`). Замечена **реорганизация рабочего дерева**: все файлы проекта перемещены из корня репозитория в подпапку `insol/` (git status: корневые файлы помечены как удалённые, `insol/` — untracked; изменение **не закоммичено**). git HEAD и CI пока держат файлы в корне, а `.github/workflows/deploy.yml` ссылается на корневые пути (`react-app`, `*.html`, `img`, `robots.txt`, `sitemap.xml`) — поэтому текущий деплой не сломан. **Важно при коммите переезда:** пути в `deploy.yml` нужно будет обновить под `insol/` (working-directory сборки React, копирование `*.html`/`img`/`robots.txt`/`sitemap.xml`), иначе сборка Pages не найдёт файлы. Обновлён путь к журналу в разделе «Регламент обновления».
- 2026-07-21 20:02 — без изменений с предыдущего обновления: новых коммитов с 19:02 нет (HEAD = `e4532ee`), реорганизация в `insol/` по-прежнему не закоммичена, каузат по `deploy.yml` из записи 19:02 остаётся в силе. Единственное: мусорный `react-app/src/.fuse_hidden0000000400000001` (отмечался 17:41/18:02) удалён — рабочее дерево чистое от хвостов редактора.
- 2026-07-21 23:02 — правка по запросу: увеличен размер участка на диаграмме пути солнца (небесный купол). В `SunPath.jsx` масштаб проекции участка поднят с `24/mx` до `48/mx` — участок отображается примерно вдвое крупнее, косая проекция и центрирование сохранены, за пределы SVG (320×250) не выходит. Также убран мусор: `.DS_Store`, `react-app/node_modules/` и `react-app/dist/` (сборочные артефакты из проверки, оба в `.gitignore`).
- 2026-07-21 23:32 — **добавлена админ-панель** (React + Radix) как отдельная страница-вход. Новое: `react-app/admin.html`, `react-app/src/admin.jsx`, `react-app/src/AdminApp.jsx`; `vite.config.js` переведён на мультистраничную сборку (`rollupOptions.input`: main + admin). Разделы: Обзор (KPI: MRR, разовые паспорта, регистрации, конверсия; графики выручки/тарифов/воронки/продукта), Пользователи (поиск + фильтры по тарифу/сегменту, ручная смена тарифа, статусы), Финансы и платежи (KPI, выручка подписки vs паспорта, лог транзакций), Продуктовая аналитика (участки/кадастр/PDF/паспорта, воронка активации «180 секунд», регистрации по месяцам), Поддержка и фидбек (баги/идеи/оценки со сменой статуса), Промокоды и тарифы (создание/выключение промокодов, тарифная линейка). Данные — детерминированный демо-генератор (`mulberry32`), структура готова под замену на реальный API. Акцент по GTM: главный доход — разовый «Паспорт участка» (790 ₽), подписка вторична. Сборка `vite build` проходит (294 модуля; отдельный бандл `admin` ~33 КБ). **Деплой:** `deploy.yml` копирует `react-app/dist` → `/app/`, поэтому после сборки админка автоматически доступна по **`/app/admin.html`** (отдельного роутинга не требуется). Артефакты сборки убраны.
- 2026-07-21 23:50 — админка дополнена двумя разделами до полного запрошенного охвата: **«Проекты участков»** (список сохранённых участков с владельцем/сегментом, регион, площадь м², число объектов, соответствие СанПиН, шаринг-ссылка, просмотры; фильтры по статусу и поиск; модерация — скрыть/вернуть; KPI: всего/расшарено/соответствие норме/на модерации) и **«Контент и блог»** (SEO-статьи по кластерам из GTM: «Куда поставить дом», «Отступы от забора», «Тень от дома», «Инсоляция и СанПиН», «Сад и огород по солнцу»; создание черновика с кластером и ключами, цикл статусов черновик→на проверке→опубликована, просмотры; KPI). Данные — детерминированные моки (проекты выводятся из тех же 64 пользователей, поэтому владельцы совпадают с разделом «Пользователи»). Навигация: 8 разделов. `vite build` проходит без ошибок (отдельный бандл `admin` ~45 КБ). Иконки Radix (`HomeIcon`, `FileTextIcon`, `Share1Icon`, `EyeOpenIcon`/`EyeNoneIcon`, `Pencil2Icon`). `node_modules`/`dist` убраны после проверки.
- 2026-07-22 00:54 — стратегия: в `gtm-roadmap.md` добавлен раздел 9 «Роадмап под ежегодное использование (ретеншен → тариф Год)». Рычаги по убыванию отдачи: сезонный садовый цикл (весна/лето/осень/зима — 3–4 захода в год), рост посадок по годам (единственная механика «из года в год», высший приоритет), проактивные сезонные напоминания (главный драйвер удержания подписки), новые проекты каждый год, живой участок + дневник A/B, окупаемость солнечных панелей по сезонам. Приоритет внедрения и честная рамка: это ставка на ретеншен (день-30 ≥ 20%, Год-1 ≥ 45%), не на первую продажу — основной доход остаётся разовый отчёт-паспорт; минимальный аккаунт только после aha-момента. Также обсуждена структурная проблема деплоя: git-репозиторий укоренён в `Sites` (на уровень выше `insol`), файлы лежат под `insol/…`, а `deploy.yml` искал `react-app` в корне → выдан фикс workflow с путями `insol/react-app` и `insol/*` (правку вносит пользователь в `Sites/.github/workflows/deploy.yml`).
- 2026-07-22 01:10 — SEO-контент: написаны 6 статей блога (те, что были заведены в разделе «Контент» админки), в формате сайта. Файлы: `article-kuda-postavit-dom.html`, `article-otstupy-ot-zabora.html`, `article-dlina-teni-ot-doma.html`, `article-ogorod-po-solncu.html`, `article-sanpin-insolyaciya.html`, `article-rasstoyanie-mezhdu-domami.html`. Каждая — под SEO: keyword-title/description/keywords, H1/H2 с целевыми запросами, внутренняя перелинковка (кластеры «куда поставить дом», «отступы от забора», «тень от дома», «инсоляция/СанПиН», «сад по солнцу», «противопожарные»), CTA на сервис, разметка Schema.org BlogPosting + FAQPage. Нормы предварительно сверены по вебу: СП 53.13330.2019 (дом 3 м, скот/птица 4 м, прочие хозпостройки 1 м, деревья 3/2/1 м), красная линия 5/3 м, санитарные 12/8/8 м, СП 4.13130.2013 табл.1 (6/8/10/12/15 м), СанПиН 1.2.3685-21 (2,5/2,0/1,5 ч, +0,5 ч при прерывистой). Обновлены `blog.html` (6 карточек вместо плейсхолдера) и `sitemap.xml` (+6 URL). Проверка: у каждой статьи ровно 1 title/H1/canonical и 2 JSON-LD; внутренние ссылки резолвятся. Статья A1 (`article-insolyaciya.html`) уже существовала — не дублировалась.
- 2026-07-22 01:50 — лендинг: на главном экране переключатель Лето/Зима с двумя фото (Summer/Winter-Shot) заменён на видео `img/landing-hero.mp4` (7.5 МБ). Тег `<video>` — autoplay/loop/muted/playsinline, poster = Summer-Shot (заставка на время загрузки). Удалены кнопки сезонов и JS-функция `setSeason`; CSS `.stage` распространён на `video`. Файлы Summer/Winter-Shot оставлены (используются как poster и в og:image). Замечание: 7.5 МБ тяжеловато для hero — при желании сжать (H.264 ~1080p, битрейт пониже) или добавить WebM-версию.
- 2026-07-22 02:05 — на hero-видео лендинга добавлен интерактивный таймбар (как в сервисе): ползунок + часы (условное время 06:00–20:00) поверх видео. Логика: при воспроизведении ползунок и часы следуют за видео (timeupdate); при перетаскивании видео ставится на паузу и проматывается по позиции ползунка (`video.currentTime`) — эффект «управления тенями/солнцем», на отпускании (pointerup) воспроизведение продолжается. Добавлены стили `.timebar/.tb-range/.tb-clock/.tb-hint` (кросс-браузерный range, градиент ночь→солнце). Проверено `node --check`.
- 2026-07-22 11:40 — заменено hero-видео лендинга на новое (загружено пользователем) и сжато ffmpeg: исходник 52.6 МБ (2136×1720, 60 fps, ~42 Мбит/с) → `img/landing-hero.mp4` 3.5 МБ (1440×1160, 30 fps, CRF 23, ~2.8 Мбит/с, без звука, yuv420p, +faststart). Ссылка в `landing.html` не менялась. Уменьшение ~в 15 раз без заметной потери качества.
- 2026-07-22 12:20 — лендинг пересобран по JTBD. Манифест переформулирован («Каждое решение на участке — про солнце»); блок «Что вы получаете» заменён на секцию «Шесть решений до первого колышка» — 6 карточек-работ (дом, баня, беседка, огород/ягоды, деревья, соседи), в каждой боль/последствие («Без проверки: …») + как решает Инсоляр. Добавлена тёмная секция «Цена ошибки» с последствиями: тёмный сырой дом (плесень, холод, счета), упадок сил/сна и дефицит витамина D, перегрев «как в теплице» (кондиционер, выгорание), нет урожая (не вызревают плоды, кислые ягоды, болезни от сырости), сожаление о посадках, спор с соседом (суд/снос). Последствия сверены по вебу (влияние дефицита света на здоровье; требования томатов/огурцов к свету). Обновлены title/description/OG под JTBD-запросы. Hero-видео с таймбаром, калькулятор, FAQ, футер сохранены. Проверено: 7/7 section, `node --check` JS OK.
- 2026-07-22 12:37 — редизайн лендинга (по запросу: структура ок, дизайн/вёрстка плохие). Выбрано направление «тёплый дачный» + сгенерированные иллюстрации → реализованы как **векторные inline-SVG** (нет доступа к растровому AI-генератору): 6 flat-иллюстраций под работы (дом, баня со «сходящим паром», беседка, грядки с томатами/огурцами, дерево с тенью, два дома+забор). Новая дизайн-система: тёплая палитра (крем/зелёный/солнечный/терракота), шрифт Manrope, скругления, мягкие тени, hero с радиальными подсветками, карточки-работы с иллюстрацией + маркерами риск(!)/решение(☀), блок «Цена ошибки» тёмно-зелёный с эмодзи-иконками, шаги, калькулятор, FAQ. Hero-видео с таймбаром и логика калькулятора сохранены без изменений. Проверка: 7/7 секций, 6/6 svg, 6 карточек, JS OK.
- 2026-07-22 14:12 — реализм 3D-сцены (React `Viewport.jsx`, three 0.160), шаг 1: добавлено IBL-окружение — `import RoomEnvironment` + `PMREMGenerator.fromScene(new RoomEnvironment(),0.04)` → `scene.environment` (мягкий заполняющий свет и лёгкие отражения на MeshStandard-материалах). Постоянные ambient/hemi приглушены (0.3→0.12, 0.5→0.3), дневной ambient в апдейте солнца 0.3→0.2, ночной 0.12→0.06 (окружение компенсирует). Солнце/тени/расчёт инсоляции не тронуты. `scene.environment.dispose()` добавлен в cleanup. Сборка `vite build` — ок. ВАЖНО: реализм визуален, отрендерить WebGL в среде нельзя — экспозицию/яркость и выбор окружения (studio RoomEnvironment vs градиентное небо) тюним по скриншотам пользователя. Дальнейшие шаги (по желанию): небо Sky-шейдером синхронно с солнцем, PBR-текстуры (трава/дерево/крыша) с normal/roughness, GLTF-деревья/дом, SSAO/SMAA через EffectComposer.
- 2026-07-22 14:40 — реализм, шаг 2 (по выбору пользователя «полный»): видимый пакет без внешних файлов — процедурные CanvasTexture. Добавлены `makeGrassTexture` (трава на земле вместо белой плоскости), `makePlasterTexture` (штукатурка на стенах дома, ExtrudeGeometry — есть UV), `makeSkyTexture(day/night)` — градиентное небо (день/ночь) вместо плоского фона; апдейт солнца переключает `scene.background` день↔ночь и красит туман по горизонту. Текстуры прокинуты через `api.current` (skyDay/skyNight/plasterTex), корректный dispose. Сборка `vite build` — ок. Осталось (визуальный чек по скрину пользователя + следующий шаг): текстура крыши/забора (крыша — кастомная BufferGeometry без UV, нужно добавить UV), нормальная модель дерева, мягкое затенение (SSAO) и сглаживание (SMAA) через EffectComposer. Напоминание: изменения не на сайте, пока не запушено; отрендерить WebGL в среде нельзя — яркость/экспозицию тюним по скрину.
- 2026-07-22 15:20 — реализм, шаг 3 (выбран путь «улучшать живую сцену», не фотореализм; референс пользователя — офлайн-рендер Blender, в браузере недостижим). Первый видимый апгрейд: деревья `kind==='tree'` переделаны с «двух шариков» на **ель ярусами** (ствол + 4 конуса убывающего радиуса, ConeGeometry) — стилизованно, тематично (дача/лес). Кусты без изменений. Сборка ок. Дальше по желанию: текстура крыши (добавить UV к gableRoofMesh), фейковые контактные тени под объектами (грунтинг), забор-текстура, лучшее сглаживание.
- 2026-07-22 15:35 — по запросу: (1) тени контрастнее — hemi 0.3→0.16, ambient день 0.2→0.1 / ночь 0.06→0.04, солнце день 1.5→2.0 / низкое 0.9→1.15, `shadow.radius` 2→1 (края резче); (2) участок теперь зелёный «как трава» — цвет plot-меша 0xf5c451→0x82a860 (свежая трава, opacity 0.92). Сборка ок. Влияние на скорость/загрузку — нулевое (только числа/цвет). Не задеплоено, пока не запушено.
- 2026-07-22 16:20 — по запросу «хочу готовые 3D-модели»: в `Viewport.jsx` подключён `GLTFLoader` — опциональная загрузка `models/tree.glb` (через `import.meta.env.BASE_URL`), клон на каждое дерево с автоподгонкой высоты (Box3), поворотом по индексу, тенями. **Безопасный фолбэк:** нет файла/ошибка → остаются процедурные ёлки-конусы (сцена не ломается). Добавлены `useState` в импорт, состояние `treeReady`, эффект загрузки, `treeReady` в deps пересборки. Создана папка `react-app/public/models/` с README (источники CC0: Kenney Nature Kit, Quaternius, Poly Haven; куда класть `tree.glb`). Бинарь модели должен добавить пользователь (скачать в среде нельзя — ограничения + лицензия). Сборка ок, `public/` копируется в `/app/models/`. Замечание: GLTF добавит вес загрузки (файл модели) — держать < 1–2 МБ.
- 2026-07-22 17:20 — стратегия: по AJTBD-промпту пользователя составлен разбор сегментов → файл `ajtbd-segments.md`. Топ-5 B2C-сегментов (СНГ): (1) застройщик ИЖС, (2) покупатель участка до сделки, (3) дачник-садовод, (4) владелец под солнечные панели, (5) частный ландшафтный дизайнер. Для каждого: Core Jobs, критерии выполнения, портрет, Big Job, TAM/SAM/SOM (оценки) и почему привлекателен. Вывод: MVP — сегменты 1+2 (транзакция), рост/ретеншен — 3 (тариф Год), маржа/дифференциация — 4+5.
- 2026-07-23 11:13 — **роза ветров, этап 1** (референс — климат-паспорт СмотриСундук, PDF пользователя). Реальные климат-данные ветра: `engine/wind.js` — `fetchWindRose(lat,lon)` тянет Open-Meteo archive (без ключа, CORS), почасовые направление/скорость за 2 года, агрегирует в 8 секторов по сезонам (зима/весна/лето/осень/год): частота, ср. скорость, штиль, господствующее направление. Компонент `three/WindRose.jsx` — полярная диаграмма (лепестки-секторы, длина = частота, цвет = ср. скорость), переключение сезонов, легенда, честная подпись «модельно, климат-частота, не прогноз». Подключено Pro-диалогом в шапке App.jsx (кнопка «🌀 Роза ветров», `windOpen`, грузит по координатам участка). Сборка ок. Дальше: этап 2 — частицы-поток ветра над 3D-сценой с господствующих направлений; этап 3 — «ветровая тень» за домом/забором (зоны затишья/продувания) + рекомендации. CFD-обтекание — вне рамок. Дифференциатор vs статичный паспорт конкурента; часть платного «паспорта участка».
- 2026-07-23 11:20 — **роза ветров, этап 2**: поток ветра частицами над 3D-сценой. В `Viewport.jsx` добавлена система из 500 точек (`THREE.Points`), в цикле рендера смещаются по вектору «куда дует» (dirDeg+180), тороидальная обёртка в пределах участка; проп `wind={{on,dirDeg}}`, обновление через `api.current.wind`, корректный dispose. В `App.jsx` — Pro-тумблер «🌬 Поток», при включении тянет розу (Open-Meteo) и берёт господствующее направление за год (`prevailingDir`) в `windDeg` (fallback 315° = СЗ). Частицы видны только при включении → на FPS без нагрузки в выключенном состоянии. Сборка ок. Осталось: этап 3 — «ветровая тень» за домом/забором (зоны затишья/продувания) + рекомендации; взаимодействие частиц со строениями пока не моделируется (летят сквозь).
- 2026-07-23 (позже) — по референсу пользователя (CFD-streamlines): поток ветра переделан с точек на **линии тока (streamlines)**. В `Viewport.jsx` добавлены `windSpeedColor` + `buildStreamlines(dirDeg, base, buildings, plotHalf)` — упрощённое потенциальное обтекание: строения аппроксимируются цилиндрами (центроид + радиус footprint), поле = равномерный поток + сумма возмущений «обтекание цилиндра» (потенциальное течение), интегрируем 26 линий по 130 точек, красим по скорости (зелёный тихо → красный быстро, ускорение по бокам препятствий). Система точек (`THREE.Points`) заменена на `windGroup` из `THREE.Line` (vertexColors); эффект перестраивает линии на смену направления/участка/строений; видимость по тумблеру «🌬 Поток». Линии огибают дом/баню/беседку. Честно: качественно (без вихрей/срыва/турбулентности), линии 1px (LineBasicMaterial; для «толстых» линий как на референсе нужен Line2/fatlines — на будущее). Сборка ок.
- 2026-07-23 (доработка линий ветра по запросу): (1) **толстые линии** — переход на `Line2`/`LineGeometry`/`LineMaterial` (fatlines, linewidth 3.5px), `resolution` из размера рендера, обновляется в `resize()`; (2) **стрелки-наконечники** — конус на конце каждой линии, повёрнут по финальному направлению ветра, цвет = скорость на конце; (3) **на всю высоту** — линии генерируются слоями по высоте от 1.4 м до макс. высоты зданий/деревьев (2–5 слоёв); выше объекта поток не отклоняется (идёт поверх) — обтекание учитывает `top` каждого препятствия; (4) **градиент** — обычная скорость жёлтый → ускорение у боков красный (`windColor`, t=(sp/U−1)/0.9). Деревья тоже стали препятствиями. Сборка ок.
- 2026-07-23 (поток циклический): линии ветра сделаны **анимированными** — по руслу «бегут» сегменты фикс. длины. `LineMaterial` переведён в `worldUnits:true` + `dashed:true` (dashSize 3.5 м, gapSize 6 м, linewidth 0.55 м), `line.computeLineDistances()`; материалы копятся в `api.current.windMats`, в цикле рендера `dashOffset -= 0.22` при `windGroup.visible` → бесконечный цикличный поток. Контраст повышен: `opacity` 1, толще, пунктир с промежутками читается динамичнее. Стрелка-конус на конце сохранена (направление). Сборка ок.
- 2026-07-23 (правки ветра по скрину): (1) убрал сплошные линии далеко за участком — поле сужено к участку: R (длина по потоку) plotHalf+18→+9, ширина «веера» = plotHalf+2, M 15→13; (2) движение сделал заметнее: сегменты короче (dashSize 3.5→2.5, gap 6→4.5), скорость `dashOffset` −0.22→−0.45/кадр. ВАЖНО: анимация пунктира была в коде уже прошлым шагом, но пользователь смотрел ещё НЕ задеплоенную (статичную fatlines) версию — движение появится после пуша этой сборки.
- 2026-07-23 (ветер → «кометы» по запросу: скругление + затухание хвоста + движение + без лишних линий вне участка): переписан рендер потока. `buildStreamlines` теперь **обрезает линии по bbox участка + отступ 7 м** (нет линий по всей траве) и возвращает пути со скоростью. Вместо толстых пунктирных Line2 — **кометы**: на каждой линии тока движется отрезок из `COMET_K=20` точек, построенный как ribbon-mesh с кастомным `ShaderMaterial` (varying `aT`: alpha = pow(1−t,1.3)·opacity → голова 100%, хвост 0%), ширина сужается к хвосту (скруглённый вид), цвет головы по скорости (жёлтый→красный). Анимация — `phase += speed` в цикле рендера, циклично (wrap по длине пути). Импорты Line2/LineGeometry/LineMaterial удалены. Тюнинг (speed 0.6, spacing 1.1, width 0.6, K 20) — по скринам. Сборка ок.
- 2026-07-22 17:35 — по запросу: написан текст лендинга под сегмент 1 (застройщик ИЖС) по JTBD-структуре пользователя (oneliner → core job+фичи → micro jobs → aha → ценности → «узнаёшь себя» → как выполним → точка Б → барьеры → увольняем конкурентов). Файл `landing-copy-zastroyshik.md`. Готово к A/B и переносу в landing.html. Сегмент в запросе не был указан — взят сегмент 1 как beachhead; для сегмента 2 (покупатель участка) можно сделать отдельный вариант.
- 2026-07-23 13:23 — по запросу «как силами пользователей наращивать ссылочную массу» в `gtm-roadmap.md` добавлен раздел **4a. Наращивание ссылочной массы силами пользователей**. Механики отсортированы по SEO-вкладу: (1) встраиваемый виджет/iframe — главный dofollow-источник, совпадает с B2B-эмбедом для риелторов; (2) бейдж «Проверено в Инсоляр»; (3) индексируемые страницы шаринга проекта; (4) UGC-галерея кейсов; (5) PDF-отчёт как носитель ссылки+QR; далее по переходам/виральности — (6) реферальная петля с двусторонним бонусом, (7) посевы силами амбассадоров (Яндекс.Кью/Пикабу/Дзен). Явно зафиксирована честная рамка: пользовательские ссылки на форумах/VK/Дзен чаще nofollow (переходы+индексация, слабый вес); настоящий вес — эмбеды и индексируемые UGC-страницы. Обязательное условие — SSR/пререндер публичных страниц (иначе краулер видит пустой JS-каркас, как у React-версии). Приоритет привязан к фазам 1–3 + метрики раздела.
- 2026-07-24 — **регистрация/вход через Яндекс ID** (по запросу «подготовь регистрацию, буду использовать яндекс регистрацию и виджет яндекса»). Сайт статический (GitHub Pages, без бэкенда) → выбран клиентский token-флоу мгновенной авторизации Яндекс ID. Создано: `react-app/src/yandexAuth.js` — ленивая загрузка SDK `sdk-suggest-with-polyfills-latest.js`, `loginWithYandex(clientId)` → `YaAuthSuggest.init({client_id, response_type:'token', redirect_uri}, origin)` → `handler()` → токен → профиль через `https://login.yandex.ru/info` (имя/e-mail/логин/аватар, graceful fallback при CORS). `react-app/public/yandex-token.html` — вспомогательная страница-приёмник (`sdk-suggest-token…js` + `YaSendSuggestToken('https://methodform.github.io',{flag:true})`), деплоится в `/app/yandex-token.html`. В `App.jsx`: `YANDEX_CLIENT_ID` (из `VITE_YANDEX_CLIENT_ID`), состояние `user` + `loginYandex`/`logoutYandex`, сессия в `localStorage`, кнопка в шапке «Войти с Яндекс ID» / профиль+выход (иконка `PersonIcon`). Проверка: esbuild-бандл `App.jsx`+`yandexAuth.js` собирается без ошибок; скобки сбалансированы. Осталось за пользователем: зарегистрировать OAuth-приложение на oauth.yandex.ru (Redirect URI `https://methodform.github.io/insolar/app/yandex-token.html`, JS Origin `https://methodform.github.io`, scopes `login:info`/`login:email`), вписать `client_id`. Инструкция — `yandex-id-setup.md`. Ограничение зафиксировано честно: клиентский вход даёт только личность; подписки/оплата остаются демо до появления бэкенда/платёжного сервиса.
- 2026-07-25 — **ветер учитывает забор** (по замечанию пользователя: забор — не сплошная стена, ветер идёт поверху и продолжается по участку, меняется лишь зона затишья). Раньше `fenceH` в поток вообще не передавался (учитывались только постройки). Добавлено в `Viewport.jsx`: `rayExitDist` (пересечение луча вверх-по-ветру с полигоном участка) и `fenceShelter(x,z,y,polyS,fenceH,fx,fz)` — забор моделируется как проницаемый барьер: НЕ отклоняет поток (линии тока не огибают участок, идут насквозь), а гасит скорость у земли с подветренной стороны; затишье тянется вглубь ~7·высот забора, затухая по глубине и по высоте (выше `fenceH` влияния нет — ветер поверху). Множитель скорости у земли прямо за забором ~0.15. Прокинут `fenceH` в `buildStreamlines` (множитель в `vel`, путь не меняется — меняется цвет/скорость комет) и `buildWindComfort` (множитель в `spd`; ранний выход убран при `fenceH>0`, чтобы забор давал зону затишья даже без построек). Вызовы обновлены (эффект уже зависел от `fenceH`). Проверка: esbuild-бандл Viewport.jsx собирается без ошибок. Не задеплоено, пока не запушено.
- 2026-07-25 — **режим ветра «Сейчас» (реалтайм)**. `engine/wind.js`: добавлена `fetchWindNow(lat,lon)` — прогнозный эндпоинт Open-Meteo (`/v1/forecast?current=wind_speed_10m,wind_direction_10m`, без ключа, CORS), возвращает `{dirDeg (откуда дует), speed м/с, time}`. `App.jsx`: переключатель в панели ВЕТЕР — «Климат (год)» ↔ «Сейчас» (`windMode`), состояние `windNow`, `loadClimate`/`loadNow`; в режиме «Сейчас» текущий ветер тянется сразу и автообновляется каждые 10 минут (`useEffect` с `setInterval`, очистка при выключении/смене). Подпись «Ветер сейчас: с <румб>, X м/с · данные на HH:MM» + честная оговорка «ближайший узел прогноза, не датчик на участке». Направление и обтекание/затишье считаются той же моделью (`compassFrom` — румб из градусов). Проверка: esbuild-бандл App.jsx + wind.js собирается без ошибок. Не задеплоено, пока не запушено.
- 2026-07-25 (фикс белого экрана) — после сборки/деплоя приложение падало с `ReferenceError: Cannot access 'ln' before initialization` (TDZ) → белый экран. Причина: добавленный `useEffect` режима «Сейчас» ссылался на `lat`/`lon` в массиве зависимостей, который вычисляется в момент рендера, а `const lat/lon` объявлены ниже по телу компонента (строка ~172). Минифицированное имя `ln` = lat/lon. Исправление: `useEffect` перенесён вниз, сразу после объявления `lat`/`lon` (и `poly`). `loadClimate`/`loadNow` обращаются к координатам только внутри стрелочных тел (отложенный вызов) — безопасны. esbuild-бандл App.jsx собирается чисто.
- 2026-07-25 — по запросу: карта по умолчанию БЕЗ схемы сторонних карт, выпадающий список убран. В `App.jsx` дефолт `ground3d` изменён `'streets'`→`'off'` (стейт без сеттера), удалён блок «Карта в 3D» (Select «Нет/Схема») и поле ввода ключа MapTiler. Земля в 3D — процедурная трава без внешних тайлов; `mapKey` остаётся в пропсах, но при `off` тайлы не грузятся. esbuild-бандл App.jsx собирается без ошибок.
- 2026-07-25 — по запросу: (1) убран режим рисования — из `App.jsx` удалён импорт `PlanEditor`, состояние `planOpen`, рендер `<PlanEditor>` и кнопка «Рисовать»; кнопка «Типовое» переименована в «Добавить объект» и растянута на всю ширину; в тарифе убрано слово «рисование» (файл `PlanEditor.jsx` остался, но больше не подключён). (2) Объекты на участке больше нельзя двигать по вертикали — из гизмо в `Viewport.jsx` удалена зелёная ось Y (`arrow(...,'ty')`); горизонтальное перемещение (tx/tz), поворот и масштаб сохранены; ветка `gd.mode==='ty'` стала недостижимой (безвредна). esbuild-бандлы App.jsx и Viewport.jsx собираются без ошибок.
- 2026-07-26 — **GTM без бюджета на рекламу + SEO-пакет.** Создано 4 документа: `gtm-strategy.md` (3 органические стратегии — A: SEO-контент, B: сообщества/партнёрства, C: продуктовая виральность — со сравнением, рекомендованным миксом и планом на 90 дней, опора на ajtbd-segments.md); `content-plan.md` (6 кластеров, календарь ~36 статей на 6 мес, форматы, ядро запросов, метрики); `seo-optimization.md` (что сделано + ручной чек-лист + инструменты); `yandex-google-setup.md` (пошагово Вебмастер + Search Console + IndexNow + Метрика, с учётом подпути /insolar/). SEO-правки в коде: в `landing.html` добавлены robots-мета, keywords, плейсхолдеры верификации (yandex-verification/google-site-verification) и расширенная Schema.org (WebSite, Organization, FAQPage — 4 вопроса) — все 4 JSON-LD валидны; `sitemap.xml` дополнен lastmod и приоритетами. Ключевая честная оговорка зафиксирована: главная /insolar/ — React (клиентский рендер, краулер видит пусто), поэтому SEO-поверхность = статический лендинг + статьи; на перспективу нужен пререндер.
- 2026-07-26 — **контент-план, батч №1: 6 статей (кластеры 1–2).** Написаны в формате блога (Inter, Schema.org BlogPosting + FAQPage, перелинковка, CTA на сервис): article-kak-raspolozhit-dom-po-solncu (столп кластера 1), article-orientaciya-okon, article-kuda-postavit-banyu, article-gde-postavit-besedku, article-orientaciya-terrasy-verandy, article-chto-postroit-na-6-sotkah. Без дублирования существующих 7 статей; новые ссылаются на них и друг на друга. Нормы — по ранее сверенным СП 53.13330.2019, СП 4.13130.2013, СанПиН 1.2.3685-21 (отступы 3/1/4 м, деревья 3/2/1, красная линия 5/3, санразрывы 12/8/8, пожарные 6–15, инсоляция 2,5/2,0/1,5 ч). Добавлены 6 карточек в blog.html (с SVG-превью) и 6 URL в sitemap.xml (lastmod 2026-07-26, приоритеты 0.6–0.8). Проверка: у каждой ровно 1 title/H1/canonical + 2 валидных JSON-LD, битых внутренних ссылок нет, все статьи есть в блоге и sitemap. Итого в блоге 13 статей. Следующий батч по плану — месяц 2 (нормы и тень). Не задеплоено, пока не запушено.
- 2026-07-26 — **правка батча №1 под обновлённый контент-план** (E-E-A-T + монетизация + первоисточники). Во все 6 статей добавлено: автор с ролью («Редакция Инсоляр · разбор по действующим нормам») и дата публикации в .meta; `datePublished`/`dateModified` и author в Schema.org BlogPosting; CTA пересобран под монетизацию — «проверить бесплатно» + мост к платному «паспорту участка», кнопка «Проверить участок бесплатно»; блок «Источники» со ссылками на первоисточники (URL сверены по вебу: СанПиН 1.2.3685-21 — docs.cntd.ru/document/573500115, СП 53.13330.2019 — 564412858, СП 4.13130.2013 — 1200101593, ссылки rel="nofollow noopener"). В «столп» (article-kak-raspolozhit-dom-po-solncu) добавлены оглавление с якорями (id у всех H2) и раздел «Цена ошибки в рублях» (отопление/переделки/урожай/спор с соседом) — по шаблону «Анатомия ранжирующейся статьи». Проверка: все JSON-LD валидны, у каждой статьи есть автор/даты/источники/CTA-мост, якоря оглавления пиллара резолвятся. Не задеплоено, пока не запушено.
- 2026-07-26 — **добавлены научные источники в 3 статьи** (по запросу — усилить обоснованность «цены ошибки» реальными данными, без выдуманных фактов; ссылки сверены по вебу). (1) article-kak-raspolozhit-dom-po-solncu: в блок «Цена ошибки» добавлен пункт «Здоровье» — дефицит дневного света (циркадные ритмы, сон, витамин D) со ссылками на Cleveland Clinic и рецензируемое исследование PMC7666121, плюс связь сырости/плесени с респираторными симптомами по руководству ВОЗ. (2) article-kuda-postavit-banyu: в раздел про просыхание добавлен абзац с данными ВОЗ (сырость/плесень ↔ кашель, свистящее дыхание, астма). (3) article-ogorod-po-solncu (существующая): к таблице света добавлен абзац о потребности томатов в 6–8 ч прямого солнца (минимум 6 для плодоношения; при нехватке — вытягивание, слабость, болезни) со ссылками на Gardener's Path и Gardening Know How. Все ссылки rel="nofollow noopener" target="_blank". Проверка: источники на месте, JSON-LD валиден. Принцип зафиксирован: только проверяемые источники, убеждение фактами, не нагнетанием (YMYL-безопасность для SEO). Не задеплоено.
- 2026-07-26 — **humanizer-проход по 6 статьям батча №1** (убрать AI-паттерны, добавить живой голос). Переписаны все 6 лидов (убран сигналинг «Разбираем…», выровненный ритм, канцелярит; добавлены разговорные интонации и лёгкий 1-е лицо). Точечно поправлены шаблонные обороты: «Классический принцип:» → живая формулировка, «золотая середина» → «тут выручает…», «Главный принцип маленького участка» → «Правило тесного участка»; заголовки «Короткий алгоритм»→«Если совсем коротко», «Короткий чек-лист»→«Коротко». Анти-AI аудит поймал новый тик: одинаковые концовки «Дальше/Ниже» в лидах — разведены (часть убрана совсем, часть заменена на «Пройдёмся по комнатам», «сейчас разберём»). Факты, нормы, ссылки, таблицы, FAQ, Schema — не тронуты. Осталось (не делал): те же паттерны в 7 старых статьях (Разбираем/золотая середина/Классический принцип) — по желанию отдельным проходом. Не задеплоено.
- 2026-07-27 — **разбор выгрузок Wordstat → seo-keywords.md.** Пользователь прислал 5 файлов (инсоляция земельного участка, расположение дома по сторонам света ×2, rus_formit/КЕО, климатический паспорт участка). Собран рабочий документ ключей по статьям с реальными частотами (точная/широкая). Главная находка: «климатический паспорт участка» — 204 показа/мес (+«бесплатно» 15, «нужен ли» 6) — прямо в монетизацию «паспорт участка» и в тему конкурента; рекомендована новая статья-столп + посадочная. Другие приоритеты: «правильное расположение дома на участке по сторонам света» (50–83) в столп кластера 1; «расположение комнат по сторонам света в частном доме» (23 точная) — раздел/статья; «инсоляция участка нормы для ИЖС» (22 точная) — H2/FAQ; tool-запросы «…онлайн / по адресу / по кадастровому номеру» — мост на сервис. КЕО-кластер (кео 438, расчёт инсоляции 228) помечен как профессиональный/чужой сегмент — брать только tool-запросы («инсоляция онлайн», «расчёт инсоляции онлайн калькулятор»). Добавлена памятка по встраиванию (title/H1/H2/FAQ/alt, без каннибализации).
- 2026-07-27 — **добавлен дамп «инсоляция» (Wordstat) в seo-keywords.md.** Зонтичное «инсоляция» — 23 492 показа, но спрос смешанный. Отобрано наше, шум отсечён. 6 «жил»: (1) tool-запросы — инсоляция онлайн (360), расчёт инсоляции онлайн (151), инсоляция по кадастровому номеру (73), калькулятор инсоляции онлайн (60) → посадочная сервиса; (2) ядро — инсоляция участка (616), инсоляция дома (409), нормы для ИЖС/СНТ; (3) определение «инсоляция простыми словами» (890/776) → верх воронки в обзорную статью, отсечь медицину пометкой «в строительстве/на участке»; (4) нормы/часы/СанПиН (нормы инсоляции 925, продолжительность 609, санпин 568); (5) тень/забор/деревья — инсоляция деревьев (124), инсоляция забора (46), «глухой забор инсоляция судебная практика» (11 — горячий, споры с соседями); (6) брендовый спрос конкурента «Смотри Сундук» (~150 суммарно) → статья-сравнение/альтернатива. Явно помечено НЕ брать: медицина (инсоляция кожи, избегать инсоляции, противопоказания) и профи-софт (ревит/архикад/солярис/КЕО/линейка). Обновлён список «первым в работу».
- 2026-07-27 — **фактчек всех 13 статей + humanizer по старым 7.** (1) Сверка фактов между статьями: противоречий нет — инсоляция 2,5/2,0/1,5 ч и зоны 58/48–58/48, периоды 22.04–22.08 / 22.03–22.09 / 22.02–22.10, дом→баня 8 м, дом→уборная 12 м, пожарные 6–15 м, красная линия 5/3, деревья 3/2/1, томаты 6–8 ч — во всех статьях одинаково. (2) Humanizer по 7 старым: убран «Разбираем…» из 6 лидов (insolyaciya, kuda-postavit-dom, dlina-teni, ogorod, sanpin, rasstoyanie) разными формулировками (объясню/покажу/по порядку/сведём в таблицу — без нового единого тика) и «Вывод простой:»→«Отсюда простое правило:» в огороде. Проверка: тиков (Разбираем/золотая середина/Классический/Вывод простой) не осталось ни в одной из 13 статей; концовки лидов у всех разные. Факты/нормы/ссылки не менялись. Не задеплоено.
- 2026-07-27 — **применён анализ Bukvarix (keywords-bukvarix.md) к статьям.** Ключевой вывод документа: SEO строить на бытовом ядре, не на «инсоляции». Вплетены точные высокочастотные фразы (естественно, без стаффинга): (1) баня — «расположение бани на участке» (1160/105) в лид + H2 «Нормы расположения бани на участке…»; (2) отступы — H2 «Отступ от забора соседа: от границы участка» (559) + «нормы застройки участка» (912) в текст; (3) тень — H2 «Куда падает тень на участке» (3065) + абзац с «тень деревьев на участке» (418) и «затенение соседнего участка» (45/10, горячий конфликтный интент); (4) столп дом — «солнечная сторона дома» (726) в Шаг 2. Факты/нормы/голос не тронуты, HTML цел. Рекомендации из Bukvarix на будущее (не сделано): отдельная статья/кластер «споры с соседями по участку» (крупный юр. пласт: границы участка споры соседей ~339, споры с соседями по участку ~225) и информационный лонгрид «что такое инсоляция простыми словами» (~9k на «это/что») как верх воронки. Не задеплоено.
- 2026-07-27 — по запросу (сайт переедет на свой сервер, «открытый вопрос» интересен) выбран **модерируемый Q&A** под статьями. Создана спецификация blog-qa-feature.md: пользовательский флоу (вопрос → очередь → одобрение+ответ автора → публикация), модель данных (таблица question), API (POST /api/qa, GET, approve/reject), антиспам без капчи (honeypot + rate-limit по хэшу IP + фильтр ссылок + ручная модерация; Yandex SmartCaptcha при необходимости — не reCAPTCHA), идентификация без иностранной авторизации (по умолчанию без входа: имя + опц. e-mail; опция — Яндекс ID через готовый yandexAuth.js). SEO: разметка QAPage (пользовательские вопросы) отдельно от редакционного FAQPage; server-render одобренных Q&A обязателен. Админка — вкладка «Вопросы» в AdminApp (раздел «Контент»). Включать не на старте, а при ≥500 визитов/мес. Мост к монетизации — ссылка «проверить участок» в ответах.
- 2026-07-27 — **разбор конкурента TimeAI Климат** (climatpassport.timeai.ai/example) → competitor-timeai-teardown.md. Прямой конкурент делает наш «паспорт участка» за 790 ₽, отчёт зрелый (8 блоков: геометрия/НСПД, спутник Esri+Wayback архив, инсоляция 3D-купол+световой день по месяцам, ветер по сезонам/месяцам+скорости/порыв/штиль, климат-профиль Open-Meteo+градусо-сутки+GDD10+окно для стройки, нормы, ИИ-рекомендации, 2 варианта размещения; + ИИ-чат, white-label PDF, A4/A3). Взять в 1-ю очередь (дёшево, наш стек уже позволяет): световой день по месяцам, восход/закат/высота на 4 сезонные даты, климат-профиль из Open-Meteo (безморозный период, отопит. сезон в градусо-сутках, снежный покров, «окно для стройки»), расширенная статистика ветра (ср. скорость по румбам, макс порыв, % штиля), «главный вывод» абзацем, длины сторон+габариты. 2-я очередь: авто-варианты размещения с плюсами/компромиссами, white-label PDF (сегмент 5 B2B), архивный спутник (но российская подложка). Позже/осторожно: ИИ-чат (нужен LLM+оговорки). НЕ копировать: иностранный Esri; не отказываться от нашего козыря — живой 3D с тенями и потоком ветра (у них статичный PDF). Стратвывод: конкурент впереди по паспорту и SEO на «климатический паспорт участка» (наш ключ 204/мес); догоняем контентом паспорта (дёшево), отрываемся интерактивом 3D + бесплатная проверка до покупки.
- 2026-07-27 — **решение о переименовании: Инсоляр → SunPlan3D**, домен sunplan3d.ru (свободен). Причина: insolar.ru занят Группой компаний ИНСОЛАР (тепловые насосы/энергоэффективность зданий + арх. бюро insolarch.ru) — смежная строительная ниша + почти идентичное написание «ИНСОЛАР»≈«Инсоляр» → риск по товарному знаку, занятый .ru и вечная SEO-конкуренция за собственное имя. Написание: логотип SunPlan3D (латиница, одно слово, = домен) + постоянный рус. слоган «Солнце, тени и ветер участка в 3D» (закрывает минус англ. имени для дачной аудитории). Замену в коде/статьях делаем ВМЕСТЕ с переездом на сервер (не сейчас). Перед стартом: проверить знак «SunPlan» в Роспатенте (классы 9, 42), по желанию забрать sunplan3d.com. Создан rebrand-checklist.md: бренд-текст (Инсоляр→SunPlan3D в лендинге/аппе/13 статьях/blog + «Редакция Инсоляр»), домен-зависимое (canonical/OG/JSON-LD/sitemap/robots/верификация/301-редиректы), Метрика/Вебмастер/SearchConsole на новом домене, Яндекс ID origin+redirect (methodform.github.io→sunplan3d.ru), CORS прокси кадастра, ЮKassa, репозиторий/деплой. SEO не страдает: продвигаемся на бытовых рус. запросах, бренд ключом быть не обязан; «3D» в имени усиливает дифференциатор.
- 2026-07-27 — **лендинг пересобран под бренд SunPlan3D** (по выбору пользователя). Айдентика: логотип-вордмарк ☀ Sun(жёлтый)Plan(тёмный)3D(азур) + мелкий слоган «солнце и тени участка в 3D»; палитра дополнена «3D/tech»-акцентом azure #2f6bd4 (+azuresoft), добавлена азуровая подсветка в hero-градиент рядом с солнечной; hero-кикер «SunPlan3D · солнце, тени и ветер участка в 3D». Бренд-текст в шапке/теле/футере, title, OG (site_name/title), 3× JSON-LD name → SunPlan3D. Структура, видео-hero с таймбаром, «6 решений», «цена ошибки», калькулятор, FAQ и вся JS-логика сохранены. Домен-зависимые URL (canonical/og:url на methodform) и остальные страницы (статьи ещё «Инсоляр») переводятся при переезде на sunplan3d.ru по rebrand-checklist. Проверка: 0 вхождений «Инсоляр», 9× SunPlan3D, теги сбалансированы, node --check inline JS OK.
- 2026-07-27 — **создана визуальная админка лендинга** landing-admin.html (по запросу — настраивать дизайн мышкой, не текстом). Standalone HTML+vanilla JS: iframe с landing.html + панель. Клик по элементу → правки: текст (поле или dblclick-inline contenteditable), размер шрифта, насыщенность (400/600/700/800), высота строки, выравнивание (left/center/right/justify), цвет текста и фона, отступы top/bottom, скрыть, выбрать родителя, сброс элемента (хранит исходный cssText в WeakMap). Глобально: базовый размер текста + CSS-переменные бренда (--sun/--green/--azure/--ink/--bg). Превью десктоп/планшет/телефон. Экспорт: «Скачать landing.html» (клонирует doc, чистит data-edsel/contenteditable/#__edstyle, сериализует с DOCTYPE) или копировать HTML; «начать заново» перезагружает iframe. Проверка: node --check JS OK, теги сбалансированы. Нюанс: открывать через локальный сервер (python3 -m http.server) или на хостинге — file:// блокирует доступ к iframe. Не задеплоено.
- 2026-07-27 — **conversion-playbook.md** (по запросу: практики конверсии + какой функционал на лендинге даёт aha → покупку). Опора на CRO-данные (интерактивное демо конвертит ~8×: 24% vs 3%; ценность до просьбы об оплате; 2–4 aha-момента). Ключевой вывод: aha = «это МОЙ участок, МОИ тени, сейчас, бесплатно» (персонализация + мгновенность + тактильность таймбара). На лендинг: (1) ввод кадастрового номера/адреса прямо в hero → сразу свой участок (главный рычаг, вынести выше); (2) серия микро-aha (тени по времени, зима/лето, вердикт СанПиН, поток ветра/затишье); (3) «цена ошибки» рядом с демо; (4) живой пример паспорта до оплаты; (5) доверие (НСПД/Open-Meteo/СП, счётчик, отзывы) — YMYL; (6) один липкий CTA. Граница free/paid: бесплатно — интерактив на экране; за 790₽ — PDF-паспорт, нормативный вердикт на расчётные даты, варианты размещения, климат+окно для стройки, шаринг/брендир. PDF, сохранение. Механика апселла: заблокированные секции паспорта прямо в бесплатном результате пользователя → «разблокировать за 790₽» (платит за уже увиденный свой участок). Метрики-воронка: заход→построил→подвигал/вердикт(aha)→дошёл до замка→оплата.
- 2026-07-27 — **прототип neighbors-proto.html** (по запросу: строить участок по точкам пользователя, кадастр опционально; соседи серыми 3D-домами). Standalone HTML: Leaflet-карта (OSM тайлы, keyless) — клик по точкам задаёт углы участка; кнопка «Построить» → центроид, локальная проекция (x=восток, z=-север), Overpass API (building в bbox+40м, CORS напрямую из браузера, без бэкенда) → фильтр «в 20 м от границы участка» (distPolyPoly). Three.js r128: земля с тенью, зелёный участок, серые соседи-коробки (ExtrudeGeometry, высота по building:levels/height или типовая floors×3м), DirectionalLight с PCFSoft-тенями, ползунки время/азимут/высота солнца + этажность, орбита мышью. Фолбэк: если OSM пусто (частая ситуация в СНТ/деревне) — сообщение «здания не нанесены, зададите вручную». Честно: солнце в прототипе задаётся вручную (в сервисе — реальная астрономия по дате из движка); высоты соседей — предположение. Проверка: node --check inline JS OK. Не задеплоено. Дальше: перенести пайплайн OSM-соседей в основной React-сервис (через прокси на будущем VPS) + реальное солнце из astronomy.js; учитывать тени соседей в расчёте инсоляции.
- 2026-07-27 — **соседние здания с карты — в основном сервисе (React)**, не отдельным прототипом. Создан engine/osm.js: fetchNeighbors(lat0,lon0,localPoly,radius=20) — Overpass (building в bbox участка+запас, CORS напрямую), конвертация lat/lon → локальные [east,north] тем же frame, что parsePoly (M_LAT=110540, mLon=111320·cos), фильтр «в 20 м от границы участка» (distPolyPoly), высота по height/building:levels или типовая (clamp 2.5–30 м). Viewport.jsx: новый проп `neighbors`, рендер серых коробок (ExtrudeGeometry, rotation.x=-π/2, castShadow/receiveShadow) в блоке перестройки сцены; neighbors добавлен в deps эффекта. App.jsx: импорт fetchNeighbors, состояние neighOn/neighbors/neighMsg, loadNeighbors (тянет по built.lat0/lon0/local, статус-сообщение, graceful при отсутствии реальных координат/пустом OSM), проп neighbors={neighOn?neighbors:[]} в Viewport, переключатель «🏘 Соседние дома с карты» в новом блоке «ОКРУЖЕНИЕ» левой панели. Соседи отбрасывают тень (участвуют в shadow map). Проверка: esbuild-бандл App.jsx+Viewport.jsx+osm.js без ошибок. Осталось (следующий шаг): учесть тени соседей в РАСЧЁТЕ инсоляции (сейчас только визуально/тень в сцене), на своём сервере — проксировать Overpass с кэшем. Не задеплоено.
