# Инсоляр — полный журнал проекта (источник правды)

> Единый документ по всей работе над сервисом: что построено, какие решения приняты,
> архитектура, стратегия, деплой, открытые задачи. Обновляется по мере работы.
>
> **Последнее обновление:** 2026-07-22 14:40 (+04)
> **Режим обновления:** ежечасно до 2026-07-22 23:00 (см. раздел «Регламент обновления»).

---

## 1. Что за продукт

**Инсоляр** — веб-сервис моделирования положения и движения солнца, теней и инсоляции
земельного участка (ИЖС/дача) в реальном времени. Пользователь задаёт участок,
расставляет постройки и озеленение, видит тени и инсоляцию, получает документ.

**Ядро ценности:** не «моделирование солнца», а снятие риска дорогой необратимой ошибки
(снос, спор с соседом, сад в тени) и получение уверенного, задокументированного решения
до начала стройки. Отличие от конкурентов — **живой интерактив** (двигаешь дом → тени
пересчитываются), а не статичный отчёт.

Живёт по адресу: https://methodform.github.io/insolar/ (ванильная версия — корень,
React-версия — `/app/`).

---

## 2. Архитектура и стек

Две параллельные версии в одном репозитории (Methodform/insolar):

- **Ванильная** (`index.html`, ~2600 строк) — single-file HTML/CSS/JS + Three.js r128 (CDN).
  Работает на GitHub Pages без сборки. Полный референс всех фич.
- **React + Radix** (`react-app/`) — Vite + React 18 + @radix-ui/themes + @radix-ui/react-icons
  + three@0.160 + maplibre-gl. Собирается в CI, деплоится в `/app/`. Идёт портирование к паритету.

Маркетинг — статические HTML: `landing.html` (лендинг, JTBD, калькулятор экономии,
переключатель лето/зима с фото), `blog.html` (блог), `article-insolyaciya.html` (первая статья).

### Структура React-приложения
- `src/App.jsx` — оболочка Radix, всё состояние и панели.
- `src/engine/astronomy.js` — чистый JS-движок (астрономия + геометрия + инсоляция), без Three.
- `src/three/Viewport.jsx` — 3D-сцена (Three.js): солнце, тени, здания, гизмо, аналитика, карта-подложка, маркеры.
- `src/three/PlanEditor.jsx` — редактор плана (вид сверху): прямоугольник/контур/дорожка, подложка-чертёж.
- `src/three/MapView.jsx` — участок на реальной карте (MapLibre + OpenFreeMap).
- `src/three/SunPath.jsx` — диаграмма пути солнца (небесный купол: сезонные дуги, стороны света, форма участка в центре, траектория текущего дня под точкой солнца).
- `src/three/HeatMap.jsx` — годовая тепловая карта (canvas). *(в UI сейчас не подключена)*
- `src/three/ZoneMap.jsx` — схема рекомендаций зонирования.
- `cadastre-proxy/worker.js` — Cloudflare Worker: участок по кадастровому номеру (НСПД).

---

## 3. Движок расчётов (`astronomy.js`)

- Позиция солнца — алгоритм Meeus/SunCalc (в UTC, ввод/вывод в местном поясе).
- Инсоляция — метод теневого полигона (без raycast): выпуклая оболочка footprint ∪ проекции тени.
- Нормы СанПиН 1.2.3685-21: непрерывная инсоляция ≥ 2.5/2.0/1.5 ч по климатическим зонам
  (север ≥58°, центр 48–58°, юг <48°).
- Отступы СП 53.13330 / СП 42.13330: дом 3 м, прочее 1 м; противопожарные СП 4.13130.
- Геометрия: площадь, offsetInward (отступ внутрь), plotBasis (базис участка по длинной стороне),
  rectFromDrag (прямоугольник вдоль участка), clampToPoly.
- Окна: facadeWindows + windowInsolation — только для дома/бани и нарисованных зданий с крышей.
- Контрольные точки (reportData): сетка 5×5 (N=4) в базисе участка, внутри контура.

---

## 4. Полный список функций

**Базовые (Free):** участок по координатам точек или по кадастровому номеру; путь солнца;
3D-тени в реальном времени; забор (светло-серый, без толщины); здания/постройки/озеленение;
инсоляция в точке и по контрольным точкам участка (зелёные/красные маркеры); длина тени;
компас С/Ю/В/З в сцене; тема светлая/тёмная/системная; участок на реальной карте (схема).

**Модели (процедурные, «+ Типовое»):** дом, баня, беседка, навес, шатёр, дерево, куст.
Быстрая посадка деревьев/кустов кликом по участку. Дорожки — в режиме «Рисовать».

**Редактор плана («Рисовать»):** прямоугольник (параллельно сторонам участка), контур с
углами 90°, дорожки (ширина), перетаскивание/поворот(R)/удаление(Del), линии отступов 1/3 м,
сетка, подложка-чертёж (загрузка картинки).

**3D-взаимодействие:** гизмо перемещения (оси вдоль сторон участка), поворот, масштаб;
перетаскивание с ограничением по нормативным отступам (объект держится в участке).

**Pro:** 3D-аналитика поверхностей («тепловизор», плавный градиент по земле/фасадам/крышам/забору,
выбор месяцев, учёт рассеянного света, легенда); нормативный отчёт по СанПиН (PDF, в футере правой
панели, «Скачать PDF отчёт»); сохранение/загрузка проекта (JSON).

**Всегда доступно в шапке:** памятка нормативных отступов (модалка), рекомендации по зонированию.

**Карта:** окно «Показать на карте» — OpenFreeMap (схема, бесплатно, без ключа); подложка-схема
в 3D — растровые тайлы MapTiler по ключу (встроенному в сборку или введённому вручную).

---

## 5. Карты и внешние данные

- **Схема улиц:** OpenFreeMap (векторные тайлы OSM) — бесплатно, без ключа, коммерция ок. Рендер MapLibre.
- **Схема-подложка в 3D:** растровые тайлы MapTiler streets (бесплатный тариф, ключ). Static Maps API
  НЕ используем — он платный (был баг «Invalid key» из-за него).
- **Спутник:** удалён из UI по решению (оставили только схему).
- **Кадастр:** Cloudflare Worker → НСПД `api/geoportal/v2/search/geoportal`, геометрия EPSG:3857 → WGS84.
  Риск анти-бота НСПД; ручной ввод точек — всегда рабочий fallback.
- **Ключ MapTiler:** встраивается в сборку через `VITE_MAPTILER_KEY` (GitHub secret `MAPTILER_KEY`),
  защищается ограничением по домену. Поле ручного ввода — fallback, когда встроенного ключа нет.
  Полностью скрыть клиентский ключ нельзя; при росте — кэш тайлов, платный тариф или self-host.

---

## 6. Деплой

GitHub Pages + GitHub Actions (`.github/workflows/deploy.yml`):
сборка `react-app` (`npm install && npm run build` с `VITE_MAPTILER_KEY`), сборка `_site`
(ванильные `*.html`, `robots.txt`, `sitemap.xml`, `img/` в корень; `react-app/dist` → `/app/`),
деплой. Репозиторий публичный, источник Pages = GitHub Actions.

**Пуш через токен (пользователь делает сам, своим PAT):**
`git push "https://Methodform:ТОКЕН@github.com/Methodform/insolar.git" main:main`
Токен: classic PAT со scope `repo` + `workflow`; в чат не вставлять.

---

## 7. Стратегия (JTBD / GTM)

Подробно — в `jtbd-insolation-service.md` и `gtm-roadmap.md`.

- **Сегменты:** частник ИЖС (эмоц. джоба «где ставить, не ошибиться»), проектировщик
  (нормативный отчёт), риелтор (презентация участка).
- **Beachhead — частник ИЖС/дача:** продукт под него готов, массовый рынок, виральность, SEO.
- **Монетизация:** щедрый Free → платные подписки. Единый модал тарифов по клику на любую платную
  функцию. Линейка: **Месяц / Сезон (6 мес, рекомендуемый) / Год** плюс разовый «паспорт участка» по
  ссылке. Акцент на **Год как центр** — «живой планировщик круглый год» (посадил дом сейчас, вернулся
  весной пересадить сад). Под подпиской: объекты/постройки, выбор даты, 3D-аналитика, PDF-отчёт,
  сохранение, кадастр по номеру.
- **Позиционирование:** «не отчёт, купленный один раз, а живой участок, который можно переиграть —
  и в любой момент выгрузить паспорт или документ по СанПиН».
- **Ключевой козырь vs конкуренты (smotrisunduk и др.):** интерактив + шаринг живой ссылкой +
  нормативный СанПиН, чего у статичных «паспортов» нет.
- **Нейминг:** обсуждались варианты под .solar и standalone (Ярило, Погожий, moy.solar, sad.solar).
  Финал не зафиксирован; текущее рабочее — «Инсоляр».
- **Конкуренты:** СИТИС Солярис (нормативка, тяжёлый десктоп), Shadowmap/SunTrace3D (3D-тени, без
  СанПиН/кадастра), smotrisunduk (потребительский PDF-паспорт с розой ветров).

## 8. SEO
Сделана техническая база: title/description/OG/Twitter-карточки, JSON-LD (SoftwareApplication/Blog/
BlogPosting), `robots.txt`, `sitemap.xml` + копирование в сборку. Осталось (пользователю):
Яндекс.Вебмастер + Google Search Console + Яндекс.Метрика; контент-план 10–15 статей под кластеры
(«куда поставить дом», «отступы от забора», «тень от дома»); при своём домене — заменить canonical/og:url.

---

## 9. Хроника ключевых правок и решений

1. Базовый прототип: солнце/тени, участок по 4 координатам, забор 1.8/2.2 м.
2. Рисование зданий по точкам (углы 90°), дефолтные точки (Самара), пресеты построек, повороты, drag.
3. Красная линия отступов 1/3 м; длина тени от забора/зданий.
4. Тёмная/светлая тема; design-guideline по photoncycle.
5. Pro-функции: годовая тепловая карта, сохранение, инсоляция по окнам; нормативный отчёт PDF/DOCX.
6. 3D-аналитика поверхностей («тепловизор»), сглаженный градиент, распространение на забор.
7. Юр. аспект: отчёт — модельный, не заменяет аккредитованную экспертизу.
8. Мобильная упаковка обсуждалась; ограничение отступов при перетаскивании; памятка отступов.
9. Плавающие панели поверх вьюпорта, скругл控1, скрытый скроллбар, псевдо-облака (позже удалены).
10. Деплой на GitHub (Pages + Actions), решены проблемы с PAT/scope/Keychain/public repo.
11. Полный переезд на React + Radix; поэтапный порт: астрономия, здания, гизмо, инсоляция, путь солнца,
    Pro, тепловая карта, отчёт, сохранение, **ручное рисование**, окна, памятка, облака+компас,
    зонирование, ограничение отступов, иконки Radix, 3D-аналитика.
12. UI-итерации: убраны вкладки; непрозрачная белая шапка/панели (panelBackground solid);
    тема свет/тьма/система; убраны облака; тонкий серый забор; фикс теней (near/far, normalBias);
    PDF в футер правой панели; маркеры окон и участка; гизмо вдоль участка; убраны лишние рамки/линии.
13. Кадастр по номеру (Worker НСПД). Реальная карта (MapLibre+OpenFreeMap) + подложка в 3D
    (сначала Static Maps — платно/баг, затем сшивка растровых тайлов). Спутник убран, оставлена схема.
14. Стратегия: JTBD-разбор, GTM-роадмап (beachhead частник), нейминг, выбор бесплатных карт, SEO-база.
15. React: гейтинг Pro-функций (объекты/постройки, выбор даты, 3D-аналитика, PDF-отчёт, сохранение —
    под Pro); забор с выбором «Нет / 1.8 м / свой»; удаление объекта клавишей DEL в 3D; убраны
    «Реальное время» и быстрые посадки деревьев/кустов кликом.
16. React, тарифы и UI-чистка: единый модал тарифов по клику на платную функцию; итерации линейки —
    Free/месяц/год → Месяц/Сезон(6 мес, рекомендуемый)/Год + разовый паспорт ссылкой → «Год как центр»
    (живой планировщик круглый год); кадастр переведён под подписку; убраны тексты про Pro, замочки на
    кнопках, кнопки «Показать на карте» и «Сбросить», разделители панелей.
17. React: мобильная адаптация — шторки (bottom sheets) панелей, нижние вкладки навигации, тач/пинч-жесты
    в 3D-вьюпорте, компактная шапка. Уточнения: полноэкранные плоские шторки без скруглений; таб-бар из 4
    вкладок (Участок/Солнце/Советы/Профиль) с центральной кнопкой 3D; тема переключается кнопкой-иконкой;
    кнопка «Построить/Build» закрывает шторку.
18. Маркетинг-ресёрч: добавлен `suntrace3d-vs-shadowmap-analysis.md` — сравнение конкурентов
    Shadowmap (зрелый лидер, ~190K визитов/мес) и SunTrace3D (инди, угол «окупаемость солнечных панелей»);
    обновляется ежечасно.
19. React, чистка UI: убрана рамка псевдоэлемента `::after` у панельных карточек (устранена лишняя
    линия в 3D-аналитике и на мобильной вёрстке).
20. React, диаграмма пути солнца и полировка тарифов/навигации: `SunPath.jsx` переработан в «небесный
    купол» — сезонные дуги, стороны света, форма участка в центре, а траектория текущего дня рисуется
    прямо под точкой солнца. Тарифы: цена в одну строку с подстрочником второй строкой, убраны галочки,
    на мобиле карточки тарифов идут вертикально. Таб-бар на мобиле — без подложек (активная вкладка
    зелёная, неактивные тёмно-серые).

---

## 10. Известные баги и как решены
- `</script>` в шаблон-строке ломает печать → экранирование `<scr${''}ipt>`.
- Разрыв тени от забора/объектов → узкий near/far теневой камеры + меньше normalBias.
- Краш при добавлении дерева → `mesh.position` только для чтения; заменено на `.position.set()`.
- «Invalid key» на 3D-подложке → MapTiler Static Maps платный; перешли на растровые тайлы.
- «Тонкая серая линия» сквозь панель → panelBackground="solid".
- Тепловая карта «полосой» → отсечение солнца ниже 5° (длинные тени у горизонта).
- Локальная сборка React невозможна (нет node_modules) → проверка `node --check` + баланс скобок, сборка в CI.
- `clampToPoly` не был импортирован в `Viewport.jsx` (используется в `plant()`) → латентный ReferenceError при посадке дерева/куста кликом. Не проявлялся, т.к. `plantMode` всегда `null` (быстрые посадки убраны). Исправлено добавлением импорта из `astronomy.js`.

## 11. Открытые задачи / роадмап
- Порт-паритет React: спутниковая подложка (осознанно убрана), 3D-«тепловизор» — сделан.
- «Паспорт участка» (потребительский PDF): план с размерами сторон/периметром/масштабом,
  сезонная инсоляция (зима/равноденствие/лето), рекомендации в PDF, обложка, лист «для заметок».
- Шаринг живой ссылкой (участок+объекты в URL) — главный интерактивный рычаг роста.
- Сохранённые сценарии / сравнение вариантов A/B.
- Онбординг к aha-моменту; мобильная адаптация.
- Кэширование тайлов карты; при росте — платный ключ/self-host.
- Монетизация: биллинг РФ, риск-пейволл, кейсы.
- SEO-контент; подключение Вебмастера/Метрики; свой домен.

---

## Регламент обновления
Документ обновляется ежечасно до **2026-07-22 23:00 (+04)** запланированной задачей:
при каждом запуске в него вносятся новые изменения проекта и переписки; после дедлайна
задача самоудаляется. Файл: `insol/project-journal.md` (проект переехал в подпапку `insol/`, см. запись 19:02).

### Журнал автообновлений
- 2026-07-21 11:02 — без изменений с предыдущего обновления (последний коммит `0c5599e` — SEO; рабочих правок нет).
- 2026-07-21 12:02 — новый коммит `3f1e923`: в React добавлен гейтинг Pro (объекты, дата, аналитика, PDF, сохранение), забор Нет/1.8/свой, удаление DEL в 3D, убраны «Реальное время» и быстрые посадки. Внесено в разделы «Хроника» (п.15) и учтено в описании функций/Pro.
- 2026-07-21 14:11 — серия новых коммитов после `3f1e923` (`a1d2ea8`, `a059c36`, `6876654`, `0ec2461`, `ea1480b`, `f8628d3`): переработка тарифов (единый модал → линейка Месяц/Сезон/Год + разовый паспорт → «Год как центр»), кадастр под подписку, чистка UI (убраны замочки, тексты про Pro, кнопки «Показать на карте»/«Сбросить», разделители панелей) и мобильная адаптация (шторки, нижние вкладки, тач/пинч). Добавлен файл `suntrace3d-vs-shadowmap-analysis.md`. Обновлены «Монетизация» (§7) и «Хроника» (п.16–18).
- 2026-07-21 15:02 — два новых коммита по мобильной адаптации React: `9eb7722` (полноэкранные шторки, таб-бар из 4 вкладок Участок/Солнце/Советы/Профиль, тема кнопкой-иконкой) и `1ae3b81` (кнопка Build закрывает шторку, плоские шторки без скруглений, центральная кнопка 3D). Оба — правки `react-app/src/App.jsx`. Уточнён п.17 «Хроника».
- 2026-07-21 16:02 — новый коммит `984244c`: убрана рамка псевдоэлемента `::after` у панельных карточек (лишняя линия в 3D-аналитике и на мобиле). Добавлен п.19 в «Хронику».
- 2026-07-21 17:51 — **обновление GTM-стратегии** по двум каналам Михаила Табунова: [@your_pet_project](https://t.me/your_pet_project) и [@bossofyourboss](https://t.me/bossofyourboss). Переписан `gtm-roadmap.md`: добавлен раздел 0 «Методология запуска» (10 принципов — DoD-позиционирование, правило 180 секунд, трафик-первый/поиск, отсекать зевак, fail-fast «отрубай», разовая vs повторяющаяся боль, бенчмарки ретеншена, экономика CAC/ROAS, доводка вайб-кода руками + буфер 80/25, пруфы). Ключевой сдвиг монетизации: у частника боль **разовая** → основной доход = **разовый платный «отчёт-паспорт» (490–990 ₽)**, подписка вторична и живёт на профи/риелторах (повторяющаяся боль). Переработаны разделы позиционирования (DoD-заголовок), каналов (сначала поиск), метрик (+ критерии «отрубить»), рисков и «5 шагов недели» (под первые продажи). Продукт признан по сути готовым — центр тяжести смещён на трафик и первые 180 секунд.
- 2026-07-21 17:41 — проверка работоспособности React-версии (по запросу, без сравнения с ванильной). Установлены зависимости, `vite build` проходит (290 модулей). Полный обзор App.jsx, Viewport/PlanEditor/SunPath/MapView/ZoneMap, движка. Найден и исправлен один реальный баг: `clampToPoly` не импортировался в `Viewport.jsx` (латентный ReferenceError в `plant()`; сейчас не стреляет — `plantMode` всегда null). Остальные компоненты и движок — без ошибок. Прим.: удаление `dist/` на смонтированном диске даёт EPERM — особенность окружения, не кода; в CI сборка с нуля. Замечен мусорный `react-app/src/.fuse_hidden0000000400000001` (хвост редактора, ничем не импортируется) — стоит удалить.
- 2026-07-21 18:02 — доучтены пять коммитов 16:33–16:52, ранее не попавшие в «Хронику» (записи 17:41/17:51 были про здоровье React и GTM): `e1e0f3e` (диаграмма пути солнца → небесный купол: сезонные дуги, стороны света, форма участка), `1d3730a` (моб. таб-бар без подложек — активная зелёная, неактивные тёмно-серые), `9672f02` (цена тарифа в одну строку + подстрочник), `435062e` (тарифы без галочек, на мобиле карточки вертикально), `e4532ee` (траектория текущего дня под точкой солнца, участок по центру). Обновлены §2 (описание `SunPath.jsx`) и «Хроника» (п.20).
- 2026-07-21 19:02 — новых коммитов с 18:02 нет (HEAD = `e4532ee`). Замечена **реорганизация рабочего дерева**: все файлы проекта перемещены из корня репозитория в подпапку `insol/` (git status: корневые файлы помечены как удалённые, `insol/` — untracked; изменение **не закоммичено**). git HEAD и CI пока держат файлы в корне, а `.github/workflows/deploy.yml` ссылается на корневые пути (`react-app`, `*.html`, `img`, `robots.txt`, `sitemap.xml`) — поэтому текущий деплой не сломан. **Важно при коммите переезда:** пути в `deploy.yml` нужно будет обновить под `insol/` (working-directory сборки React, копирование `*.html`/`img`/`robots.txt`/`sitemap.xml`), иначе сборка Pages не найдёт файлы. Обновлён путь к журналу в разделе «Регламент обновления».
- 2026-07-21 20:02 — без изменений с предыдущего обновления: новых коммитов с 19:02 нет (HEAD = `e4532ee`), реорганизация в `insol/` по-прежнему не закоммичена, каузат по `deploy.yml` из записи 19:02 остаётся в силе. Единственное: мусорный `react-app/src/.fuse_hidden0000000400000001` (отмечался 17:41/18:02) удалён — рабочее дерево чистое от хвостов редактора.
- 2026-07-21 23:02 — правка по запросу: увеличен размер участка на диаграмме пути солнца (небесный купол). В `SunPath.jsx` масштаб проекции участка поднят с `24/mx` до `48/mx` — участок отображается примерно вдвое крупнее, косая проекция и центрирование сохранены, за пределы SVG (320×250) не выходит. Также убран мусор: `.DS_Store`, `react-app/node_modules/` и `react-app/dist/` (сборочные артефакты из проверки, оба в `.gitignore`).
- 2026-07-21 23:32 — **добавлена админ-панель** (React + Radix) как отдельная страница-вход. Новое: `react-app/admin.html`, `react-app/src/admin.jsx`, `react-app/src/AdminApp.jsx`; `vite.config.js` переведён на мультистраничную сборку (`rollupOptions.input`: main + admin). Разделы: Обзор (KPI: MRR, разовые паспорта, регистрации, конверсия; графики выручки/тарифов/воронки/продукта), Пользователи (поиск + фильтры по тарифу/сегменту, ручная смена тарифа, статусы), Финансы и платежи (KPI, выручка подписки vs паспорта, лог транзакций), Продуктовая аналитика (участки/кадастр/PDF/паспорта, воронка активации «180 секунд», регистрации по месяцам), Поддержка и фидбек (баги/идеи/оценки со сменой статуса), Промокоды и тарифы (создание/выключение промокодов, тарифная линейка). Данные — детерминированный демо-генератор (`mulberry32`), структура готова под замену на реальный API. Акцент по GTM: главный доход — разовый «Паспорт участка» (790 ₽), подписка вторична. Сборка `vite build` проходит (294 модуля; отдельный бандл `admin` ~33 КБ). **Деплой:** `deploy.yml` копирует `react-app/dist` → `/app/`, поэтому после сборки админка автоматически доступна по **`/app/admin.html`** (отдельного роутинга не требуется). Артефакты сборки убраны.
- 2026-07-21 23:50 — админка дополнена двумя разделами до полного запрошенного охвата: **«Проекты участков»** (список сохранённых участков с владельцем/сегментом, регион, площадь м², число объектов, соответствие СанПиН, шаринг-ссылка, просмотры; фильтры по статусу и поиск; модерация — скрыть/вернуть; KPI: всего/расшарено/соответствие норме/на модерации) и **«Контент и блог»** (SEO-статьи по кластерам из GTM: «Куда поставить дом», «Отступы от забора», «Тень от дома», «Инсоляция и СанПиН», «Сад и огород по солнцу»; создание черновика с кластером и ключами, цикл статусов черновик→на проверке→опубликована, просмотры; KPI). Данные — детерминированные моки (проекты выводятся из тех же 64 пользователей, поэтому владельцы совпадают с разделом «Пользователи»). Навигация: 8 разделов. `vite build` проходит без ошибок (отдельный бандл `admin` ~45 КБ). Иконки Radix (`HomeIcon`, `FileTextIcon`, `Share1Icon`, `EyeOpenIcon`/`EyeNoneIcon`, `Pencil2Icon`). `node_modules`/`dist` убраны после проверки.
- 2026-07-22 00:54 — стратегия: в `gtm-roadmap.md` добавлен раздел 9 «Роадмап под ежегодное использование (ретеншен → тариф Год)». Рычаги по убыванию отдачи: сезонный садовый цикл (весна/лето/осень/зима — 3–4 захода в год), рост посадок по годам (единственная механика «из года в год», высший приоритет), проактивные сезонные напоминания (главный драйвер удержания подписки), новые проекты каждый год, живой участок + дневник A/B, окупаемость солнечных панелей по сезонам. Приоритет внедрения и честная рамка: это ставка на ретеншен (день-30 ≥ 20%, Год-1 ≥ 45%), не на первую продажу — основной доход остаётся разовый отчёт-паспорт; минимальный аккаунт только после aha-момента. Также обсуждена структурная проблема деплоя: git-репозиторий укоренён в `Sites` (на уровень выше `insol`), файлы лежат под `insol/…`, а `deploy.yml` искал `react-app` в корне → выдан фикс workflow с путями `insol/react-app` и `insol/*` (правку вносит пользователь в `Sites/.github/workflows/deploy.yml`).
- 2026-07-22 01:10 — SEO-контент: написаны 6 статей блога (те, что были заведены в разделе «Контент» админки), в формате сайта. Файлы: `article-kuda-postavit-dom.html`, `article-otstupy-ot-zabora.html`, `article-dlina-teni-ot-doma.html`, `article-ogorod-po-solncu.html`, `article-sanpin-insolyaciya.html`, `article-rasstoyanie-mezhdu-domami.html`. Каждая — под SEO: keyword-title/description/keywords, H1/H2 с целевыми запросами, внутренняя перелинковка (кластеры «куда поставить дом», «отступы от забора», «тень от дома», «инсоляция/СанПиН», «сад по солнцу», «противопожарные»), CTA на сервис, разметка Schema.org BlogPosting + FAQPage. Нормы предварительно сверены по вебу: СП 53.13330.2019 (дом 3 м, скот/птица 4 м, прочие хозпостройки 1 м, деревья 3/2/1 м), красная линия 5/3 м, санитарные 12/8/8 м, СП 4.13130.2013 табл.1 (6/8/10/12/15 м), СанПиН 1.2.3685-21 (2,5/2,0/1,5 ч, +0,5 ч при прерывистой). Обновлены `blog.html` (6 карточек вместо плейсхолдера) и `sitemap.xml` (+6 URL). Проверка: у каждой статьи ровно 1 title/H1/canonical и 2 JSON-LD; внутренние ссылки резолвятся. Статья A1 (`article-insolyaciya.html`) уже существовала — не дублировалась.
- 2026-07-22 01:50 — лендинг: на главном экране переключатель Лето/Зима с двумя фото (Summer/Winter-Shot) заменён на видео `img/landing-hero.mp4` (7.5 МБ). Тег `<video>` — autoplay/loop/muted/playsinline, poster = Summer-Shot (заставка на время загрузки). Удалены кнопки сезонов и JS-функция `setSeason`; CSS `.stage` распространён на `video`. Файлы Summer/Winter-Shot оставлены (используются как poster и в og:image). Замечание: 7.5 МБ тяжеловато для hero — при желании сжать (H.264 ~1080p, битрейт пониже) или добавить WebM-версию.
- 2026-07-22 02:05 — на hero-видео лендинга добавлен интерактивный таймбар (как в сервисе): ползунок + часы (условное время 06:00–20:00) поверх видео. Логика: при воспроизведении ползунок и часы следуют за видео (timeupdate); при перетаскивании видео ставится на паузу и проматывается по позиции ползунка (`video.currentTime`) — эффект «управления тенями/солнцем», на отпускании (pointerup) воспроизведение продолжается. Добавлены стили `.timebar/.tb-range/.tb-clock/.tb-hint` (кросс-браузерный range, градиент ночь→солнце). Проверено `node --check`.
- 2026-07-22 11:40 — заменено hero-видео лендинга на новое (загружено пользователем) и сжато ffmpeg: исходник 52.6 МБ (2136×1720, 60 fps, ~42 Мбит/с) → `img/landing-hero.mp4` 3.5 МБ (1440×1160, 30 fps, CRF 23, ~2.8 Мбит/с, без звука, yuv420p, +faststart). Ссылка в `landing.html` не менялась. Уменьшение ~в 15 раз без заметной потери качества.
- 2026-07-22 12:20 — лендинг пересобран по JTBD. Манифест переформулирован («Каждое решение на участке — про солнце»); блок «Что вы получаете» заменён на секцию «Шесть решений до первого колышка» — 6 карточек-работ (дом, баня, беседка, огород/ягоды, деревья, соседи), в каждой боль/последствие («Без проверки: …») + как решает Инсоляр. Добавлена тёмная секция «Цена ошибки» с последствиями: тёмный сырой дом (плесень, холод, счета), упадок сил/сна и дефицит витамина D, перегрев «как в теплице» (кондиционер, выгорание), нет урожая (не вызревают плоды, кислые ягоды, болезни от сырости), сожаление о посадках, спор с соседом (суд/снос). Последствия сверены по вебу (влияние дефицита света на здоровье; требования томатов/огурцов к свету). Обновлены title/description/OG под JTBD-запросы. Hero-видео с таймбаром, калькулятор, FAQ, футер сохранены. Проверено: 7/7 section, `node --check` JS OK.
- 2026-07-22 12:37 — редизайн лендинга (по запросу: структура ок, дизайн/вёрстка плохие). Выбрано направление «тёплый дачный» + сгенерированные иллюстрации → реализованы как **векторные inline-SVG** (нет доступа к растровому AI-генератору): 6 flat-иллюстраций под работы (дом, баня со «сходящим паром», беседка, грядки с томатами/огурцами, дерево с тенью, два дома+забор). Новая дизайн-система: тёплая палитра (крем/зелёный/солнечный/терракота), шрифт Manrope, скругления, мягкие тени, hero с радиальными подсветками, карточки-работы с иллюстрацией + маркерами риск(!)/решение(☀), блок «Цена ошибки» тёмно-зелёный с эмодзи-иконками, шаги, калькулятор, FAQ. Hero-видео с таймбаром и логика калькулятора сохранены без изменений. Проверка: 7/7 секций, 6/6 svg, 6 карточек, JS OK.
- 2026-07-22 14:12 — реализм 3D-сцены (React `Viewport.jsx`, three 0.160), шаг 1: добавлено IBL-окружение — `import RoomEnvironment` + `PMREMGenerator.fromScene(new RoomEnvironment(),0.04)` → `scene.environment` (мягкий заполняющий свет и лёгкие отражения на MeshStandard-материалах). Постоянные ambient/hemi приглушены (0.3→0.12, 0.5→0.3), дневной ambient в апдейте солнца 0.3→0.2, ночной 0.12→0.06 (окружение компенсирует). Солнце/тени/расчёт инсоляции не тронуты. `scene.environment.dispose()` добавлен в cleanup. Сборка `vite build` — ок. ВАЖНО: реализм визуален, отрендерить WebGL в среде нельзя — экспозицию/яркость и выбор окружения (studio RoomEnvironment vs градиентное небо) тюним по скриншотам пользователя. Дальнейшие шаги (по желанию): небо Sky-шейдером синхронно с солнцем, PBR-текстуры (трава/дерево/крыша) с normal/roughness, GLTF-деревья/дом, SSAO/SMAA через EffectComposer.
- 2026-07-22 14:40 — реализм, шаг 2 (по выбору пользователя «полный»): видимый пакет без внешних файлов — процедурные CanvasTexture. Добавлены `makeGrassTexture` (трава на земле вместо белой плоскости), `makePlasterTexture` (штукатурка на стенах дома, ExtrudeGeometry — есть UV), `makeSkyTexture(day/night)` — градиентное небо (день/ночь) вместо плоского фона; апдейт солнца переключает `scene.background` день↔ночь и красит туман по горизонту. Текстуры прокинуты через `api.current` (skyDay/skyNight/plasterTex), корректный dispose. Сборка `vite build` — ок. Осталось (визуальный чек по скрину пользователя + следующий шаг): текстура крыши/забора (крыша — кастомная BufferGeometry без UV, нужно добавить UV), нормальная модель дерева, мягкое затенение (SSAO) и сглаживание (SMAA) через EffectComposer. Напоминание: изменения не на сайте, пока не запушено; отрендерить WebGL в среде нельзя — яркость/экспозицию тюним по скрину.
