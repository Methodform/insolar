# Инсоляр — полный журнал переписки (источник правды по сессии)

> Полная запись переписки и работы по проекту **Инсоляр** (сервис моделирования солнца,
> теней и инсоляции участка). Каждый пункт: что попросил пользователь → что сделано/отвечено →
> результат и открытые вопросы. Технические детали проекта — в `project-journal.md`.
>
> **Последнее обновление:** 2026-07-22 14:04 (+04)
> **Режим обновления:** ежечасно до **2026-07-22 23:00 (+04)** задачей `insolar-perepiska-update`;
> после дедлайна — финальная запись и самоудаление задачи.

---

## 0. Контекст проекта
Инсоляр — веб-сервис (GitHub Pages, репозиторий `Methodform/insolar`): ванильная версия в корне
`insol/`, React+Radix в `insol/react-app/` → деплой в `/app/`. Beachhead — частник ИЖС/дача.
Локально проект лежит в `…/Sites/insol`, но **корень git-репозитория — папка `Sites`** (на уровень выше).

---

## 1. Проверка работоспособности React-версии
- **Запрос:** продолжаем работу; сначала предлагалась тепловая карта — пользователь отказался,
  попросил проверить React-версию на баги.
- **Сделано:** установлены зависимости, `vite build` — проходит (290 модулей). Полный обзор
  `App.jsx`, `Viewport/PlanEditor/SunPath/MapView/ZoneMap`, движка `astronomy.js`.
- **Найден и исправлен баг:** `clampToPoly` использовалась в `Viewport.jsx` (`plant()`), но не была
  импортирована → латентный `ReferenceError`. Не проявлялся (посадки кликом отключены), импорт добавлен.
- **Итог:** остальной код без ошибок; правки по тепловой карте откачены.

## 2. Чистка мусора
- **Запрос:** «сделай всё, чтобы не было мусора».
- **Сделано:** удалены `react-app/src/.fuse_hidden…` (хвост редактора), `.DS_Store`, а также
  временные `react-app/node_modules` и `react-app/dist` (создавались для проверки; в `.gitignore`).
- Для удаления на смонтированном диске потребовалось разрешение (EPERM) — выдано.

## 3. Диаграмма пути солнца
- **Запрос:** участок на диаграмме движения солнца маленький — увеличить.
- **Сделано:** в `SunPath.jsx` масштаб участка поднят (24 → 48 → **72 px**).

## 4. Админ-панель сервиса
- **Запрос:** сделать админ-панель; уточнили — все разделы, внутри React, на моковых данных.
- **Состояние:** базовая админка (`admin.html`, `src/admin.jsx`, `src/AdminApp.jsx`, мультистраничный
  `vite.config.js`) уже была заскаффолжена; в ней 6 разделов (Обзор, Пользователи, Финансы,
  Продуктовая аналитика, Поддержка, Промокоды/тарифы).
- **Сделано мной:** добавлены недостающие запрошенные разделы — **«Проекты участков»** (список
  сохранённых участков, фильтры, модерация, KPI) и **«Контент и блог»** (SEO-статьи по кластерам,
  создание/цикл статусов). Итого 8 разделов, всё на детерминированных моках.
- **Проверка:** `vite build` — ок; headless-рендер (jsdom+React) всех 8 разделов — 0 ошибок.
- Деплой админки: `/app/admin.html`.

## 5. Стратегия: ежегодное использование
- **Запрос:** что можно сделать для ежегодного использования сервиса.
- **Отвечено + сделано:** в `gtm-roadmap.md` добавлен раздел 9 «Роадмап под ежегодное использование
  (ретеншен → тариф Год)»: сезонный садовый цикл, рост деревьев по годам (главная механика),
  сезонные напоминания, новые проекты, дневник участка, панели по сезонам. С приоритетами и честной
  рамкой (ставка на ретеншен, а не на первую продажу).

## 6. Тарифы: обсуждение
- **Вопрос пользователя:** убрать «паспорт», оставить полугодовой и годовой тариф?
- **Отвечено:** это разворот главной ставки (боль частника разовая). Ключевой вопрос — авто-продление
  или разовая оплата за период. Рекомендация: Сезон+Год как разовая оплата за период, документ —
  внутри тарифов, не убирать дешёвый вход совсем; решать по A/B (цифрам).
- **Открыто:** B2B/корпоративный тариф для профи (архитекторы, кадастр, риелторы) — в продукте
  (модал оплаты + админка PLANS) не заведён. Обсуждали структуру (единый «Бизнес» vs по сегментам)
  и цену-ориентир (~4 990 ₽/мес). **Решение не зафиксировано — ждём выбора.**

## 7. SEO-статьи блога
- **Запрос:** написать статьи, заведённые в разделе «Контент» админки; статьи под SEO.
- **Сделано:** написаны 6 статей (A1 «Инсоляция…» уже была) в формате сайта:
  `article-kuda-postavit-dom.html`, `article-otstupy-ot-zabora.html`, `article-dlina-teni-ot-doma.html`,
  `article-ogorod-po-solncu.html`, `article-sanpin-insolyaciya.html`, `article-rasstoyanie-mezhdu-domami.html`.
  Каждая: keyword-title/description, H1/H2, внутренняя перелинковка, Schema.org BlogPosting+FAQPage.
- **Нормы сверены по вебу:** СП 53.13330.2019 (дом 3 м, скот/птица 4 м, прочие хозпостройки 1 м,
  деревья 3/2/1 м), красная линия 5/3 м, санитарные 12/8/8 м, СП 4.13130.2013 (6/8/10/12/15 м),
  СанПиН 1.2.3685-21 (2,5/2,0/1,5 ч).
- Обновлены `blog.html` (6 карточек) и `sitemap.xml` (+6 URL).

## 8. Деплой: разбор проблемы (важно)
Последовательность заливки на GitHub и почему сайт не собирался:
1. **Утёк токен** в чат/скрин → рекомендовано отозвать и создать новый.
2. **Ошибка URL:** пропал `@` перед `github.com` → git принял токен за порт. Дан вариант с токеном
   в переменной `TOKEN`.
3. **404 GitHub Pages** → деплой падал.
4. **Actions красные:** шаг «Build React app» падал за 0 сек. Ложный след — пустой `package.json`
   (оказался следствием запуска git не из той папки).
5. **Истинная причина:** корень git-репозитория — `Sites`, файлы лежат под `insol/…`, а `deploy.yml`
   искал `react-app` в корне → «working directory react-app: No such file or directory».
- **Решение (передано пользователю):** переписать `Sites/.github/workflows/deploy.yml` на пути
  `insol/react-app` и `insol/*` (копирование). **Пользователь должен внести эту правку — без неё сайт
  не пересоберётся.** Правильные URL: сайт `…/insolar/`, админка `…/insolar/app/admin.html`.

## 9. Лендинг: видео на главном экране
- **Запрос:** заменить два фото (лето/зима) на видео.
- **Сделано:** переключатель Лето/Зима заменён на `<video>` (`img/landing-hero.mp4`, autoplay/loop/muted).
- **Таймбар:** добавлен интерактивный таймбар поверх видео — перетаскивание проматывает ролик
  (тени идут за солнцем), при воспроизведении ползунок следует за видео; часы 06:00–20:00.
- **Новое видео + сжатие:** пользователь загрузил новый ролик (52.6 МБ) → сжат ffmpeg до **3.5 МБ**
  (1440×1160, 30 fps, CRF 23, без звука, faststart), заменил `img/landing-hero.mp4`.

## 10. Лендинг: пересборка по JTBD + редизайн
- **Запрос:** пересобрать лендинг по JTBD — боли: где строить дом, баню, беседку; где сажать
  огурцы/помидоры/ягоды; где сажать деревья; как не затенять соседей. Последствия: тёмный дом,
  плесень, холод / жара как в теплице, невызревание плодов (+ найти ещё причины).
- **Сделано (JTBD-контент):** секция «Шесть решений до первого колышка» (6 карточек-работ с болью и
  решением) + тёмная секция «Цена ошибки» (последствия). Доп. причины сверены по вебу: дефицит света →
  плохой сон/настроение, дефицит витамина D; избыток → перегрев, кондиционер, выгорание; тень →
  невызревание, кислые ягоды, болезни растений.
- **Запрос:** «дизайн и вёрстка плохие, что нужно?» → выбраны **тёплый дачный стиль** + иллюстрации.
- **Сделано (редизайн):** тёплая палитра, шрифт Manrope, воздух, мягкие тени/скругления; 6 векторных
  **SVG-иллюстраций** под работы (растрового AI-генератора в среде нет); карточки с маркерами
  риск(!)/решение(☀); блок последствий с иконками. Hero-видео с таймбаром и калькулятор сохранены.

## 11. Анализ конкурента GeoRisk (geo-risk.ru)
- **Запрос:** какие методы использует geo-risk.ru для определения границ участка.
- **Сделано:** разобран сайт + сетевой ответ (пользователь прислал JSON).
- **Вывод:** источник границ — **НСПД** (nspd.gov.ru), тот же, что у нашего `cadastre-proxy`.
  Признаки в ответе: `categoryName:"Земельные участки ЕГРН"`, `externalKey`, `interactionId`, схема
  `options`. GeoRisk **проксирует НСПД через свой бэкенд с кэшем** (поля `cacheHit`, `summary`,
  `rawProperties`). Границы не вычисляют — берут отмежёванную геометрию из ЕГРН.
- **Рекомендации для нас:** (1) добавить кэш в наш Worker (Cloudflare KV/Cache, ключ = кад. номер) —
  снимает анти-бот/лимиты НСПД; (2) проверить проекцию: ответ приходит в градусах WGS84 при лейбле
  `EPSG:3857` — убедиться, что воркер не делает двойной пересчёт (тест на `63:32:1603003:3535`).

---

## Открытые задачи (по состоянию на 13:45, 22.07)
1. **Правка `deploy.yml`** (пути `insol/…`) — за пользователем; без неё деплой падает. *[критично]*
2. **Пуш накопленных изменений** (статьи, blog, sitemap, роадмап, лендинг, видео, админка).
3. **B2B/корпоративный тариф** — выбрать структуру и цену, завести в модал оплаты и админку.
4. **Кадастр:** добавить кэш в `cadastre-proxy/worker.js`, проверить проекцию координат.
5. Отозвать утёкший GitHub-токен, если ещё не сделано.

---

## Журнал автообновлений переписки
- 2026-07-22 13:45 — документ создан; зафиксирована вся переписка сессии (пп. 1–11) и открытые задачи.
- 2026-07-22 14:04 — без изменений с предыдущего обновления: новых реплик в транскрипте сессии нет (последнее сообщение — создание этого документа в 13:45), новых записей в `project-journal.md` нет (HEAD журнала — 12:37), файлы в `insol/` с 12:37 не менялись. Открытые задачи без изменений.
